## Assets/Game/Scripts/Vehicles/DuneFoil/Sail/SailAerodynamics.cs:136-153,210-217,269-280; DuneFoil/Foil/FoilLift.cs:285-296,328-343; DuneFoil/Foil/FoilPhysics.cs:91-107; DuneFoil/Sail/SailSurface.cs:236-241,308-317; DuneFoil/Sail/SailRig.cs:180-183; DuneFoil/Core/DuneFoilLocomotion.cs:148-153,277-284; Stations/DuneFoilHelm.cs:255-265,304-311; Stations/DuneFoilMooring.cs:1-13; Stations/DuneFoilRiggingStation.cs:201-205,218-225; Stations/DeckBoarding.cs:95-99; Systems/WalkerPlatformCarrier.cs:12-24,240-247; DesertCrawler/DesertCrawlerLocomotion.cs:17-25  [-180 / +15 (net -165)]
- **smell**: Changelog-in-comments. 1720 of 6782 lines in this scope (25.4%) are comment lines, and a large share is bug post-mortem narrative rather than explanation of the code as it stands: "This was wrong in two ways at once", "This used to leave HasGround true forever after the first successful probe", "Nothing caught it because the tests only pinned that main and jib disagree", "The first version drove localEulerAngles.z", "measured once at 293 m from the player spawn", "the mast rake control shipped rotating about a local axis that happened to be right". That history belongs in git, and it is the largest single block of removable text in the domain.
- **change**: Delete the post-mortem paragraphs listed. Keep the one-sentence invariant each of them was protecting — e.g. FoilLift.ProbeGround keeps "skip our own colliders and any non-kinematic body: the craft must not ride on its own passengers" and drops the 11 lines describing the climbing-on-a-rider loop; SailAerodynamics.SailForce keeps "angle of attack is measured from edge-on (0) to broadside (90)" and drops the 18-line <remarks>. No code touched, no behaviour touched.
- **risk**: Low. Zero behaviour change. The judgement call is which rationale is load-bearing; erring toward keeping a one-liner per block (budgeted above) avoids re-introducing any of the documented bugs.

## Assets/Game/Scripts/Vehicles/DuneFoil/Core/BoardingRamp.cs:34; DuneFoil/Core/DuneFoilLocomotion.cs:115-142; DuneFoil/Core/WindField.cs:59,119-122; DuneFoil/Foil/FoilLift.cs:120,129,144; DuneFoil/Foil/FoilPhysics.cs:82,200; DuneFoil/Foil/FoilRudder.cs:69,72; DuneFoil/Sail/SailRig.cs:67,74-83,185-189,198-207; DuneFoil/Sail/SailSurface.cs:173; Stations/DuneFoilMooring.cs:42; Stations/DuneFoilRiggingStation.cs:74,77,80; Ornithopter/Wings/OrnithopterWingAnimator.cs:120; Rover/Core/RoverMovementController.cs:55-57; Tools/CrawlerToolRig.cs:207-215  [-95 / +0]
- **smell**: Speculative public surface. A member-level scan of every .cs under Assets/ (matching each declared public name against every other file, including Editor/ and Tests/) found 25 public members in this scope with zero consumers anywhere: BoardingRamp.IsDown; DuneFoilLocomotion.ForwardSpeed/Speed01/YawRate/RudderCommand; WindField.DirectionFrom/SpeedAt; FoilLift.SurfaceY/IsFoiling/IsStalling; FoilPhysics.MaxGrade/GripLimitedYawRate (internal-only); FoilRudder.SteerAngle01/MaxSteerAngle; SailRig.Jib-adjacent SetArea/HoistAll/AnyHoisted; SailSurface.Cant; DuneFoilMooring.IsMoored; DuneFoilRiggingStation.IsEasing/IsTrimming/Function; OrnithopterWingAnimator.IsInitialised; RoverMovementController.SetMoveSpeed/SetRotationSpeed/GetCurrentSpeed; CrawlerToolRig.SnapToTargets. Several are also mis-documented — CrawlerToolRig.SnapToTargets says "Snap every tool to its current target without easing. For editor previews" but writes only the smoothing state and never calls Apply(), so it cannot pose anything in edit mode where LateUpdate does not run.
- **change**: Delete the 25 members and their doc comments. Demote FoilPhysics.MaxGrade and GripLimitedYawRate to private (used only inside FoilPhysics). A further 8 members are test-only (DuneFoilLocomotion.Halt/IsHelmDriven, WindField.SetWind, SailRig.MainSail, FoilRudder.Wheelbase setter, OrnithopterWingAnimator.SnapToFolded, OrnithopterWingRig.Cradle/TailHub) — keep those, they are the harness seams the tests need.
- **risk**: Low, but these are the readouts a HUD would naturally want. Speed01, IsFoiling and IsStalling in particular look like they were written for a gauge that was never built — if a foiling/stall indicator is on the roadmap, keep those three (12 lines) and cut the other 22.

## Assets/Game/Scripts/Vehicles/DuneFoil/Foil/FoilLift.cs:320-400 vs Assets/Game/Scripts/Locomotion/Ground/WalkerGround.cs:51-106  [-80 / +20 (net -60), and FoilLift inherits WalkerGround's buffer-growth fix]
- **smell**: Duplicated ground probe, with the shared version already extracted. FoilLift.ProbeGround re-implements WalkerGround.Ray line for line: RaycastNonAlloc downward, reject hits where `transform.IsChildOf(self)`, reject hits whose attachedRigidbody is non-kinematic, take the best surface. Both files carry a long comment about the same failure (a craft climbing into the sky on its own passenger). WalkerGround.cs:5-7 explicitly says this logic "was duplicated almost verbatim between the ostrich and the walking station, which is two places for the same bug to hide" — and FoilLift is a third place. FoilLift's copy is also the weaker one: its buffer is a fixed `new RaycastHit[24]` with no growth, whereas WalkerGround documents at length (lines 29-36, 59-69) that a saturated RaycastNonAlloc silently drops arbitrary hits including the ground, and grows the buffer up to 512 to prevent it.
- **change**: Add "SpaceGame.Locomotion" to the (currently empty) references array in Assets/Game/Scripts/Vehicles/DuneFoil/SpaceGame.Vehicles.DuneFoil.asmdef. Add one `ceiling` parameter overload to WalkerGround.Ray implementing FoilLift's maxGroundStep acceptance window plus its lowest-above fallback (~14 lines in WalkerGround), then delete FoilLift.ProbeGround and its probeHits field entirely, calling WalkerGround from SampleGround and SampleSlope.
- **risk**: Medium and cross-domain — WalkerGround is shared by every legged machine, so the new overload must not change the existing Ray path (add an overload, do not modify the 2-arg one). Also fixes bug #1 below as a side effect only if the ceiling sentinel is normalised to +Infinity; verify with the existing DuneFoilCraftTests and the walker locomotion EditMode suite. Coordinate with the Locomotion auditor.

## Assets/Game/Scripts/Vehicles/Rover/Core/RoverBogieIK.cs:2,155,217; Stations/DuneFoilRiggingStation.cs:5; Systems/WalkerPlatformCarrier.cs:27; Tools/CrawlerToolRig.cs:29; Tools/CrawlerToolModule.cs:28; Parts/ArticulatedPart.cs:13  [-8 / +0]
- **smell**: Debug scaffolding and dead usings. Two unconditional `Debug.Log` calls fire from RoverBogieIK.SetupWheelPhysics per wheel — and InitializePhysics is called from BOTH Awake() and OnEnable(), so on first activation each of the 3 wheel mounts logs twice, 6 lines of console spam per bogie (RoverNoHierarchy.prefab has 3 bogies = 18 lines). Separately, `using SpaceGame.Vehicles.Crawler;` is unused in DuneFoilRiggingStation.cs, WalkerPlatformCarrier.cs, CrawlerToolRig.cs and CrawlerToolModule.cs — the only public type in that namespace is DesertCrawlerLocomotion and grep shows it appears in those four files only inside comments. `using SpaceGame.Locomotion;` in RoverBogieIK.cs and `using SpaceGame.Agents;` in ArticulatedPart.cs are likewise unreferenced.
- **change**: Delete the two Debug.Log lines (or gate them behind a serialized verbose flag), and delete the 6 unused using directives.
- **risk**: None. Unused usings are a compile-time no-op; the Debug.Logs carry no information a developer needs at runtime.

## Assets/Game/Scripts/Vehicles/Stations/DuneFoilHUD.cs:137-138 vs DuneFoil/Sail/SailRig.cs:119-120,64  [-6 / +0]
- **smell**: Recomputed derived state. SailRig already publishes `InNoGoZone`, computed as `SailAerodynamics.IsInNoGoZone(heading, -TrueWind.normalized)`. DuneFoilHUD re-derives the same answer from a different input (`off < SailAerodynamics.NoGoHalfAngle` on the APPARENT wind bearing), so the HUD's "IN IRONS" warning and the rig's own InNoGoZone flag can disagree — and `SailRig.InNoGoZone` has no other reader, so it is a public property nothing consumes while its one natural consumer computes its own version.
- **change**: Either have DuneFoilHUD read `rig.InNoGoZone` (drops 2 lines from the HUD), or delete `SailRig.InNoGoZone` and its computation (drops 4 lines from SailRig). The apparent-vs-true distinction is deliberate per the HUD's comment at lines 116-119, so deleting SailRig.InNoGoZone is the honest cut.
- **risk**: Low. If netcode or AI ever needs "can this craft sail its current heading", InNoGoZone is the right place for it — but nothing asks today.

## Assets/Game/Scripts/Creatures/Horse/CanterGait.cs:178-221 + Assets/Game/Scripts/Locomotion/Policy/GaitPatterns.cs:166  [-44 / +0]
- **smell**: Self-documented copy-paste forced by an access modifier. CanterGait duplicates GaitLayout.RippleOrder and GaitLayout.Centre verbatim, and says why in a 6-line comment: 'GaitLayout in SpaceGame.Locomotion does this for the shipped patterns and is internal to that assembly, so these are its two rules restated.'
- **change**: Change `internal static class GaitLayout` to `public static class GaitLayout` (one token), delete CanterGait.cs lines 178-221 including the apology comment, and point CanterGait.Bind at GaitLayout.RippleOrder / GaitLayout.Centre.
- **risk**: None. Pure arithmetic, already covered by CanterGaitTests and LocomotionPolicyTests. GaitLayout has no state.

## Assets/Game/Scripts/Creatures/Ostrich/OstrichSpineMotion.cs:76-119,229-236; Assets/Game/Scripts/Creatures/Ostrich/OstrichNeckMotion.cs:106-181; Assets/Game/Scripts/Creatures/Humanoid/HumanoidSpineMotion.cs:91-150,193-201; Assets/Game/Scripts/Creatures/Horse/HorseSpineMotion.cs:114-178,299-308  [-125 / +36]
- **smell**: Four copies of the same bone-rig boilerplate. Each of the four spine/neck components independently implements: rediscover-a-Transform[]-by-name-prefix-if-any-entry-is-null (3× `NeedsRediscovery` + 1× `Rediscover`, each carrying the same 4-line comment about re-exported bone counts), find-the-bone-named-Head, capture-rest-localRotations, build-a-normalised-bend-weight-curve (HorseSpineMotion:137-145 and OstrichNeckMotion.BuildWeights:167-181), and spread-one-rotation-over-the-chain (3× `SpreadOverNeck`, one even and two weighted — the weighted form subsumes the even one at weights=1/n).
- **change**: One new file Assets/Game/Scripts/Locomotion/Rig/BoneChain.cs (~36 lines, in SpaceGame.Locomotion which all four asmdefs already reference) exposing: `Transform[] Chain(Component owner, Transform[] cached, string prefix)`, `Transform Bone(Component owner, Transform cached, string name)`, `Quaternion[] Capture(Transform[] bones)`, `float[] Weights(int n, float bias, bool towardHead)`, `void Spread(Transform[] bones, float[] weights, Quaternion rotation)`. Delete all four local copies.
- **risk**: Low. All four Resolve() methods are already public and dt-driven for tests (OstrichNeckMotionTests, HorseLocomotionTests, HumanoidArmSwingTests exercise them). Only real care point: OstrichSpineMotion's SpreadOverNeck uses Slerp(identity, rotation, 1/n) per joint while HorseSpineMotion uses Slerp(identity, rotation, bendWeights[i]) — the shared form must be the weighted one, called with a uniform weight array for the ostrich, or the ostrich's peck amplitude changes.

## Assets/Game/Scripts/Creatures/Ostrich/OstrichSpineMotion.cs:149-160 and Assets/Game/Scripts/Creatures/Horse/HorseSpineMotion.cs:256-267  [-24 / +11]
- **smell**: Identical 12-line ApplyCounterSwing in two files: read body.eulerAngles.y, DeltaAngle against lastYaw / dt, normalise by locomotion.MaxYawRate, clamp to ±1, multiply by -amount*speed01, Lerp with a hard-coded 6f smoothing constant, SpreadOverNeck about Vector3.forward. Both carry the same 'taken from how fast the body is ACTUALLY turning rather than from a commanded rate' rationale.
- **change**: Add `static float CounterSwing(ref float smoothed, ref float lastYaw, Transform body, float maxYawRate, float amount, float speed01, float dt)` to the BoneChain helper above; both call sites become one line plus their own Spread call.
- **risk**: Low. The hard-coded 6f is the same in both, so nothing needs a new serialized field.

## Assets/Game/Scripts/Creatures/Ostrich/OstrichSpineMotion.cs:170-181 and Assets/Game/Scripts/Creatures/Humanoid/HumanoidSpineMotion.cs:204-217  [-26 / +10]
- **smell**: Identical ApplyLook in two files: flatten MeasuredVelocity, SignedAngle against forward, clamp to ±maxLookAngle, scale by lookAheadWeight, exponential-Lerp into smoothedLook, then `head.rotation = AngleAxis(smoothedLook, up) * head.rotation`. The only difference is that one reads a cached `body` field and the other reads `transform` — the same object.
- **change**: Fold into the shared helper as `void Look(ref float smoothed, Transform head, Transform body, Vector3 velocity, float maxAngle, float weight, float smooth, float dt)`.
- **risk**: Low.

## Assets/Game/Scripts/Creatures/Ostrich/OstrichSpineMotion.cs:135; Assets/Game/Scripts/Creatures/Ostrich/OstrichNeckMotion.cs:200; Assets/Game/Scripts/Creatures/Horse/HorseSpineMotion.cs:195; Assets/Game/Scripts/Creatures/Humanoid/HumanoidSpineMotion.cs:167; Assets/Game/Scripts/Creatures/Humanoid/HumanoidArmSwing.cs:151  [-10 / +2]
- **smell**: The expression `Mathf.Clamp01(Mathf.Abs(locomotion.LastFrame.AchievedSpeed) / Mathf.Max(locomotion.MaxSpeed, 1e-4f))` is written out five times in five files. It is a property of the locomotion, not of its observers.
- **change**: Add `public float SpeedFraction => Mathf.Clamp01(Mathf.Abs(diagnostics.AchievedSpeed) / Mathf.Max(MaxSpeed, 1e-4f));` next to the other read-back properties in LeggedLocomotion.cs (~215) and replace all five sites.
- **risk**: None. Note it is NOT the same as the existing protected `RunBlend`/`Effort`, which read the commanded twist rather than the achieved speed — keep both.

## Assets/Game/Scripts/Creatures/Drivers/{OstrichDriver,HumanoidDriver,HorseDriver}.cs + Assets/Game/Scripts/Agents/AI/Motors/LeggedDriver.cs:442-447  [-75 / +18]
- **smell**: Three drivers that are the same file with the nouns changed. Each declares `[Range(0,1)] float walkThrottle` (0.45/0.35/0.35), `bool autoWalk`, and the identical 10-line `Idle()` override, plus the identical 3-line 'There is deliberately no keyboard fallback' comment. CrabDriver.cs (21 lines) and Vehicles/Drivers/DesertCrawlerDriver.cs (19 lines) are already empty subclasses, which is the shape the other three should have.
- **change**: Move `walkThrottle`, `autoWalk` and the `Idle()` override up into LeggedDriver under a `[Header("Idle")]`. The three subclasses shrink to CrabDriver's shape (a `[RequireComponent]` and a comment). Field names are unchanged, so the values serialized on Ostrich.prefab / HorseRobot.prefab / HumanoidRobot.prefab survive — Unity keys plain [SerializeField] data by field name, and the base is the same object.
- **risk**: Low. A more aggressive version — make LeggedDriver concrete and delete all five subclasses (-172 lines, 5 files, 5 metas) — requires repointing m_Script GUIDs in 6 prefabs and loses the [RequireComponent] guard that stops a driver being added to a machine with no locomotion. Not recommended at this stage; the field-lift gets most of the win at none of the risk.

## Assets/Game/Scripts/Locomotion/Policy/StrideModels.cs:72-73  [-1 / +1]
- **smell**: Dimensionally inconsistent floor. `Mathf.Sqrt(Mathf.Max(linkage * 0.15f, linkage * linkage - height * height))` compares a LENGTH (linkage*0.15) against an AREA (L²-h²) inside one Max. The floor therefore scales as sqrt(L) rather than as L, so it is not scale-invariant — which contradicts LeggedLocomotion.Rig.cs:3-7 ('rescaling the model re-tunes the whole machine with no numbers to edit') and is the number HumanoidLocomotion.cs:29-35 spends 7 comment lines reasoning about.
- **change**: Either `linkage * linkage * 0.15f * 0.15f` (a true 15%-of-reach floor) or state in the comment that the term is deliberately not an area. One token either way. Whichever is chosen, LocomotionPolicyTests.cs:73 (`new HipBudgetStride(1.0f, 0.72f)`, the floor case) pins the current value and would need its expected number updated.
- **risk**: Behaviour change on any rig standing tall enough to hit the floor. Both shipping bipeds sit inside their usable range (ostrich 0.86, humanoid 0.90 against a floor reached at 0.930), so no shipping machine's stride moves — but confirm before merging by running LocomotionPolicyTests and HumanoidLocomotionTests.

## Assets/Game/Scripts/Creatures/Horse/HorseSpineMotion.cs:249  [-1 / +3 (or -1 / +6 with the accessor)]
- **smell**: A tuning threshold duplicated as a magic number across an assembly boundary. `trotQuiet = 1f - Mathf.Clamp01(1f - Mathf.Abs(speed01 - 0.5f) * 4f)` hard-codes the trot window as 0.25..0.75 of top speed, but CanterGait already owns that transition as the serialized `walkToTrot` (0.45) and `trotToCanter` (0.55) on HorseLocomotion. The nod's quiet band is five times wider than the actual trot plateau, and retuning either serialized field silently desynchronises the head from the gait it is supposed to be locked to.
- **change**: Expose the blend CanterGait already computes (it has `SuspensionFraction`; add or reuse a `TrotBlend(runBlend)` accessor) and drive trotQuiet from it, or at minimum move 0.25/0.75 into serialized fields next to nodAngle so the two can be kept in step by hand.
- **risk**: Low, display-only. Justified as a net-positive because the alternative is a number that goes wrong silently.

## Assets/Game/Scripts/Locomotion/Ground/WalkerGround.cs:163  [-2 / +1]
- **smell**: Stale comment that misdescribes the only live code path. `Sample()` is labelled 'Older two-out form, kept for the ostrich' but it is now the sole foothold-sampling entry point for all five machines (LeggedLocomotion.Rig.cs:262-263 -> ground.Sample), while the supposedly-current `TrySurface` has no external caller at all.
- **change**: Swap the roles in the docs: `Sample` is the API, `TrySurface` becomes a private helper. Combine with the `clearAbove` removal above.
- **risk**: None.

## Assets/Game/Scripts/World/Streaming/Core/WorldStreamer.cs:360-389 (PreloadChunksAroundPosition) and :1050-1055 (GetChunksInRadius)  [-34 / +2]
- **smell**: Duplicated implementation of the same operation. `PreloadChunksAroundPosition(Vector3, Action)` and `PreloadChunksAroundPositions(IEnumerable<Vector3>, Action)` are the same 25-line algorithm twice (resolve coord -> chunks in radius -> filter NotLoaded -> countdown EnqueueLoad -> WhenChunkContentBuilt). Only the plural version got the off-world guard, which is also the bug below.
- **change**: Replace the whole singular body with a delegation: `public void PreloadChunksAroundPosition(Vector3 worldPos, Action onComplete = null) => PreloadChunksAroundPositions(new[] { worldPos }, onComplete);`. I diffed the two bodies line by line — the plural version is a strict superset (it additionally null-checks config and skips off-world positions), so the delegation is behaviour-preserving apart from fixing the clamp. `GetChunksInRadius` then has only one caller left at line 410, where `AddChunksInRadius` can be used directly against the existing HashSet, so the helper (and its throwaway HashSet allocation per position) goes too.
- **risk**: Low. Both call sites (PlayerController.cs:58 offline preload, InteriorManager.cs:263 interior exit) pass an in-world position in the normal case, so ordinary play is unchanged; the only behaviour change is the intended one.

## Assets/Game/Scripts/World/Streaming/Core/WorldStreamer.cs:34-35 and :1148-1151  [-8 / +0]
- **smell**: Dead serialized toggle plus an empty method. `[SerializeField] private bool showDebugGizmos = true;` is read in exactly one place — `OnDrawGizmos()`, whose entire body is `if (!showDebugGizmos || config == null || config.chunks == null) return;` and then nothing. The gizmo drawing it used to guard is gone; the inspector still shows a 'Debug / Show Debug Gizmos' checkbox that does nothing.
- **change**: Delete the `[Header("Debug")]` + field and delete `OnDrawGizmos()` entirely. Keep `OnValidate()` (it clamps agentSnapDistance and is real).
- **risk**: None. Removing a SerializeField only drops a serialized value from prefabs/scenes; nothing reads it.

## Assets/Game/Scripts/World/Streaming/Core/WorldStreamer.cs:37-41 and :1139  [-7 / +0]
- **smell**: Dead public event with a doc comment that instructs callers to use it. `public event Action OnInitialChunksReady;` — documented 'Subscribe to this before spawning players' — has ZERO subscribers. I grepped the identifier across every .cs in Assets (excluding WorldStreamer.cs itself): 0 hits. The `initialChunksLoaded` flag it accompanies IS live (the `InitialChunksLoaded` property is read by LoadingScreenUI.cs), and NetworkGameManager instead uses the `onComplete` callback of PreloadChunksAroundPositions.
- **change**: Delete the event declaration + its 5-line doc comment and the `OnInitialChunksReady?.Invoke();` line in MarkInitialChunksLoaded (which then just sets the flag).
- **risk**: None — no subscriber exists. It is a public API on a NetworkBehaviour, so if the team considers it intended-future-API, leave it; but as written it is a documented hook nobody uses.

## Assets/Game/Scripts/World/ProceduralGeneration/Cave/Profiles/CaveMaterialSettings.cs:17-18  [-3 / +0]
- **smell**: Dead toggle / unimplemented feature promise. `public Material chamberMaterial;` with the tooltip 'Optional second material applied via submesh to BigChamber/Cathedral rooms. Null = primary material is used for everything.' I scanned every serialized field in all 11 World settings/profile classes for reads outside their own declaration; this is the only one with zero reads. Confirmed: the identifier appears in exactly 7 places in the whole project — its declaration, and `chamberMaterial: {fileID: 0}` in all 6 CaveProfile assets. Nothing in CaveSpawner or the meshers ever builds the submesh the tooltip describes.
- **change**: Delete the field + tooltip. (Or implement it — but as shipped it is a designer-facing inspector slot that silently does nothing.)
- **risk**: None; every existing asset has it null anyway. Removing a public field from a [Serializable] class drops the key from the 6 CaveProfile assets on next save.

## Assets/Game/Scripts/World/Environment/Props/Ship.cs:1  [-1 / +0]
- **smell**: Stray `using Unity.VisualScripting;` in a 29-line file that uses no Visual Scripting type — a leftover of Rider/Unity auto-import. It creates a compile-time dependency on the com.unity.visualscripting package from a trivial win-condition script.
- **change**: Delete the using line. Worth grepping for the same auto-import across the rest of the repo — this is the kind of thing that appears in bulk.
- **risk**: None.

## Assets/Game/Scripts/World/Environment/Sky/DayNightCycle.cs (whole file)  [-2 / +0 (plus whitespace)]
- **smell**: Bad formatting: 4-space-then-8-space mixed indentation, trailing whitespace on ~20 lines, and two stray whitespace-only lines between the `cycleDuration` and `startTimeOfDay` fields (lines 11-13) that render as a gap in the inspector. Same pattern in PostProcessVisor.cs and GlassDistortionRenderFeature.cs — all three look like they came from the same non-conforming source.
- **change**: Run the project's formatter over the three Environment/Visor + Environment/Sky files. Pure whitespace.
- **risk**: None.

## ProjectSettings/EditorBuildSettings.asset (2 rows)  [-8 / +0 (YAML rows)]
- **smell**: Stale rows. Two of the 263 enabled entries point at scene paths that do not exist on disk: Assets/Game/Scenes/Interiors/TestInterior.unity and Assets/Game/Scenes/Interiors/InsideRuin.unity. I verified with os.path.exists over all 263 entries — these are the only two missing. No code loads either by name (grep for the names across Assets finds only a UI label string in CutsceneDoor.prefab:511 and an unrelated GameObject name in InteriorTestBootstrap.cs:74).
- **change**: Delete the two entries. Do this in the same pass as the 192 orphan chunk rows so the file is cleaned once.
- **risk**: None — the files are already gone.

## Assets/Game/Scripts/World/ProceduralGeneration/Settlement/Core/SettlementLayout.cs:89 (Generate) and Detail/SettlementDetailPlacer.cs (whole file)  [0 / 0 (informational)]
- **smell**: God method / god class, reported for visibility rather than as a proposed edit. SettlementLayout.Generate is a SINGLE 291-line method (the file is 405 lines with 5 methods total) — the worst single-method offender in this domain. SettlementDetailPlacer is 931 lines / 23 methods with PlaceRooftop at 98 lines and PlacePillarClusters at 74. WorldStreamer is 1156 lines / 51 methods but is genuinely partitioned by ASCII-banner sections and reads fine.
- **change**: NO CHANGE PROPOSED. Extracting methods here is net line-POSITIVE and the brief says adding 'better architecture' is a failure. Flagging only so the orchestrator knows where the density is if someone later touches settlement generation. If anything is done, the cheapest real win is that both SettlementLayout.Generate and SettlementDetailPlacer are reachable only from SettlementBuilder/SettlementSpawner, which appear in exactly 3 chunk scenes (Chunk_7_5, the orphan Chunk_18_10, and FerdinandChunk_3_1) — so after the orphan sweep the whole 5-file settlement subsystem (~2,300 LOC) serves 2 scenes. Worth asking the user whether it is still active work.
- **risk**: n/a

## Assets/Game/Scripts/World/ProceduralGeneration/Cave/Meshing/MarchingCubesMesher.cs:161-171 vs Terrain/Meshing/TerrainMarchingCubesMesher.cs:291-299  [-14 / +8]
- **smell**: Copy-pasted helpers between the two marching-cubes implementations. `Idx(x,y,z,nx,ny)` is byte-identical in both; `InterpolateVertex` / `Interpolate` are identical apart from the name. TerrainMarchingCubesMesher's own doc comment says 'Adapted from the cave system's MarchingCubesMesher'.
- **change**: Move the two 4-line helpers onto the already-shared `MarchingCubesTables` static class (both files already reference it for EdgeTable/TriTable/CornerOffsets/EdgeCorners) and delete both local copies. I checked and do NOT recommend merging the two meshers: the terrain one adds a per-column surface-band cache, an Unsampled sentinel, and gradient normals read from the cache instead of re-sampling the field, and the two use opposite triangle winding on purpose (cave normals point inward, terrain outward). Unifying them would add code, not remove it.
- **risk**: Low but the payoff is tiny — only worth doing if someone is already in these files.

## Assets/Game/Scripts/Agents/AI/Motors/IMovementMotor.cs:19-25, RigidbodyMotor.cs:240-250, FlyingRigidbodyMotor.cs:276-286, HoverRigidbodyMotor.cs:176-187, NavMeshAgentMotor.cs:220-240, LeggedDriver.cs:254-259, OrnithopterFlightMotor.cs:76-77  [-66 / +0]
- **smell**: Dead interface surface implemented six times. IMovementMotor declares NudgeDestination(Vector3) and SuggestDestination(Vector3); all six motors implement them; nothing ever calls either. The doc comment even names a caller that does not do it: "Drive the agent toward a position set externally (e.g. by HerdModule destination sharing)" — HerdModule returns MoveIntents and never touches the motor API.
- **change**: Delete both methods from IMovementMotor and from all six implementations. Verified safe: grep -rn 'NudgeDestination|SuggestDestination' over every .cs finds only declarations/definitions and zero invocations, and the same grep over .unity/.prefab/.asset finds zero hits, so no UnityEvent persistent call reaches them by name either.
- **risk**: None. Purely subtractive, no call sites, no serialized references. Note LeggedDriver is one of the four load-bearing locomotion GUIDs — editing its body is fine, only renaming the type is forbidden.

## Assets/Game/Scripts/Agents/Modules/Flocking/FlockingModule.cs (whole file), Assets/Game/Scripts/Agents/Controller/AgentController.cs:17-20,37-40,87-105, Assets/Game/Scripts/Agents/AI/Core/AgentContext.cs:2,20-24  [-141 / +0]
- **smell**: An abandoned first attempt at group movement (superseded by HerdModule) is still dragging a per-frame physics broadphase query through the live agent hot path. AgentController.BuildContext runs Physics.OverlapSphereNonAlloc plus up to 32 GetComponent<IMovementMotor>() interface lookups whenever nearbyAgentScanRadius > 0, and PatrolRobot 1/2/3 ship with nearbyAgentScanRadius: 30 serialized — so three live prefab types (spawned from Chunk_17_10 and SettlementConfig) pay a 30 m sphere overlap plus 32 interface GetComponents every frame each, for data whose only consumer is on zero prefabs.
- **change**: Delete FlockingModule.cs; delete AgentController's nearbyAgentScanRadius/nearbyAgentLayer fields with their [Header]/[Tooltip], the three 32-element reused buffers (neighbourBuffer/nearbyPositionBuffer/nearbyVelocityBuffer), and the whole `if (nearbyAgentScanRadius > 0f)` block in BuildContext; delete AgentContext.NearbyAgentPositions/NearbyAgentVelocities/NearbyAgentCount and the stale header comment on line 2. Also drop AgentContext.IsImmobile in the same pass — AgentController.cs:83 writes it and grep for 'context.IsImmobile' across all .cs returns zero readers (Motor.IsImmobile is read directly at line 68 for the animator).
- **risk**: None behaviourally — FlockingModule is on no prefab, so the scan result is written and never read. Orphaned `nearbyAgentScanRadius`/`nearbyAgentLayer` YAML keys on 30+ prefabs are silently dropped by Unity on next save. If the user wants to keep FlockingModule as a feature, the minimum change is to zero nearbyAgentScanRadius on PatrolRobot 1/2/3 instead.

## Assets/Game/Scripts/Agents/Perception/PerceptionModule.cs:12,33-43,45-50,52,64,77-90,94-104,133,161-168,183  [-45 / +1]
- **smell**: PerceptionModule maintains a complete second copy of the target memory that AgentTargeting now owns, and nobody reads it. It exposes LastKnownPosition / HasLastKnownPosition / TimeSinceLastSeen / MemoryDuration and burns an Update() every frame keeping them current, but every consumer (ChaseModule.cs:89-91, SearchModule.cs:52-54) reads AgentTargeting's versions instead — grep confirms zero external reads of the PerceptionModule properties. Separately, NotifySpotted(Transform) has zero call sites, and it is the sole consumer of the emitNoiseOnSpot / spotNoiseRadius / playSpotSound / spotSound serialized fields, the noiseEmitter field, and the `using FMODUnity` import. HasLineOfSight(Transform) also has zero external callers (AgentRangedCombatModule has its own same-named private wrapper that calls HasLineOfSightFrom directly).
- **change**: Delete NotifySpotted plus the four On-Spot/Audio serialized fields, the noiseEmitter field and its Awake lookup, and the FMODUnity import. Delete the three memory properties, the memoryDuration field and MemoryDuration accessor, the memory half of Update() (lines 79-86) and the three memory writes in CanSee — CanSee then collapses to `public bool CanSee(Transform target) => IsVisible(target);` and AgentTargeting keeps working unchanged because it already owns memory. Delete HasLineOfSight(Transform). Keep the isMoving computation in Update (movingFovBonus reads it) and all of IsVisible/HasLineOfSightFrom.
- **risk**: Low-medium. This deletes serialized fields from a component on 5 live prefabs; Unity drops the orphan keys, and no code path reads them. The one judgement call: if the user ever intends to hook NotifySpotted up to the alert channel, this removes the hook — but see the AlertBroadcaster removal, the whole channel is currently inert.

## Assets/Game/Scripts/Agents/Modules/Movement/FleeModule.cs:67-86, Assets/Game/Scripts/Agents/Modules/Movement/KeepDistanceModule.cs:73-90, Assets/Game/Scripts/Agents/AI/Brains/NpcBrain.cs:209-229, Assets/Game/Scripts/Agents/Modules/Combat/AgentRangedCombatModule.cs:232-254  [-59 / +14 (net -45); -38 / +14 (net -24) if NpcBrain is deleted first]
- **smell**: Four copies of "pick a NavMesh point directly away from X". FleeModule.TryGetFleeDestination, KeepDistanceModule.TryGetPositionAwayFrom and NpcBrain.TryGetPositionAwayFrom are structurally identical: `away = self - threat; away.y = 0; if (away.sqrMagnitude < 0.0001f) away = Random.insideUnitSphere with y=0; candidate = self + away.normalized * distance; NavMesh.SamplePosition(candidate, out hit, sampleDistance, AllAreas)`. AgentRangedCombatModule.BackAwayTo is the same shape anchored on the target instead of self.
- **change**: Add one static `public static bool TryPointAwayFrom(Vector3 from, Vector3 threat, float distance, float sampleDistance, out Vector3 result)` to the existing static class TargetResolution.cs (no new file), and replace the three identical private helpers with calls to it. Leave AgentRangedCombatModule.BackAwayTo alone — its target-anchored variant plus its StopAndFace-on-failure fallback are genuinely different behaviour and folding it in would add branches, not remove them.
- **risk**: Low. Three call sites, all in the same assembly and namespace, all with identical semantics. Do it after the NpcBrain decision so the work is not done twice.

## Assets/Game/Scripts/Agents/AI/Core/MoveIntent.cs:21-22,57,65-66; RigidbodyMotor.cs:296-301; HoverRigidbodyMotor.cs:276; NavMeshAgentMotor.cs:342-347  [-19 / +1]
- **smell**: MoveIntent carries two generations of the facing mechanism side by side. The older FacingDirection + OverrideFacingDirection pair is never enabled: grep of every MoveTo( call site across Assets shows none passes the `overrideFacingDirection` parameter (all use positional args up to speedMultiplier, or the named `isRunning:`), so OverrideFacingDirection is always false and the three motor branches that test it are unreachable. The live channel is FacePosition + OverrideFacing + WithFacing, arbitrated by AgentController.ApplyFacingOverride and IFacingModule.
- **change**: Delete the FacingDirection and OverrideFacingDirection fields, the `overrideFacingDirection`/`facingDirection` parameters of MoveIntent.MoveTo and their two assignments; delete the dead first branch of NavMeshAgentMotor.ApplyMoveIntent (so `else if (intent.OverrideFacing)` becomes the first branch — same behaviour), the dead branch in RigidbodyMotor, and simplify HoverRigidbodyMotor.cs:276 to `FaceDirection(moveDirection, deltaTime)`.
- **risk**: Low. MoveIntent is a plain struct passed by value, not a SerializeReference type, so nothing is stored by name. Verified no call site enables the flag, so no behaviour changes.

## Assets/Game/Scripts/Agents/Animation/AgentAnimatorDriver.cs:56-67  [-11 / +0]
- **smell**: Six public animator-trigger helpers with zero callers, plus their private helper. TriggerHurt, TriggerDie, TriggerShootRifle, TriggerSpearAttack, TriggerByName and SetIsAiming are dead: grep for each across every .cs, .prefab and .unity returns only the declaration (so no code call, and no UnityEvent persistent call by method name), and grep across .anim/.controller/.fbx returns nothing (so no AnimationEvent invokes them by name either). The live agents set animator params directly instead — HealthReactionModule.cs:108/122 uses animator.SetTrigger(hurtAnimTrigger/dieAnimTrigger), AgentRangedCombatModule.cs:350/413 uses shootAnimTrigger and aimAnimBool.
- **change**: Delete all six methods and SetTriggerSafe. Keep Tick(). While there, note the inconsistency this exposes: HealthReactionModule's dieAnimTrigger defaults to "Death" while the dead TriggerDie used "Die" — deleting the dead one removes the ambiguity.
- **risk**: None. Verified no C#, UnityEvent or AnimationEvent reference. AgentAnimatorDriver has 21 prefab refs but only Tick() is used, called from AgentController.cs:68.

## Assets/Game/Scripts/Agents/Modules/Combat/TurretModule.cs:214-238 and Assets/Game/Scripts/Agents/Modules/Combat/RocketLauncherTurret.cs:68-91  [-34 / +12 (net -22)]
- **smell**: Fire() is copy-pasted between the two turrets, including the same LogWarning text pattern and the same LateUpdate comment. Both do: null-check projectilePrefab and warn; resolve `spawn` through the same two-level `muzzle/gunBarrel != null ? ... : (rotatingHead/rotatingPart != null ? ... : transform)` ternary; Instantiate at spawn with LookRotation(spawn.forward); GetComponent<TurretProjectile>() and AddComponent if missing; tp.Init(damage, gameObject); GetComponent<Rigidbody>() and AddComponent if missing; set useGravity and linearVelocity = dir * speed.
- **change**: Add one static `public static void Launch(GameObject prefab, Transform spawn, float speed, int damage, GameObject shooter, bool useGravity)` to the existing TurretProjectile.cs and have both Fire() bodies call it after resolving `spawn`. No new file.
- **risk**: Low — two call sites, identical semantics, both in the same assembly. Lower priority than the items above; do it only if RocketLauncherTurret survives (it is a live gadget, see moves).

## Assets/Game/Scripts/Agents/Core/EntityTargetRegistry.cs:103-115; Assets/Game/Scripts/Agents/Modules/Facing/InteractionFocusModule.cs:13; Assets/Game/Scripts/Agents/Modules/Flocking/HerdModule.cs:118,183  [-19 / +1]
- **smell**: Small dead members scattered through otherwise live files. EntityTargetRegistry.HasAny(owner, required) has zero callers anywhere (13 LOC of an O(n) scan). InteractionFocusModule.IsFocused has zero readers. HerdModule's single-argument GetSlotPositionAround(Vector3) overload has zero callers — ChaseModule.cs:94 always uses the two-argument form. HerdModule.ModuleDescription (line 183) documents "separationRadius / separationStrength" fields that do not exist on the class, so the inspector help text is wrong.
- **change**: Delete EntityTargetRegistry.HasAny, InteractionFocusModule.IsFocused and the one-arg GetSlotPositionAround overload; fix the last bullet of HerdModule.ModuleDescription to name combatSpreadRadius instead of the two non-existent fields. Also collapse the duplicate private FindChildByName helper that appears verbatim in CloseCombatModule.cs:118-123 and AgentRangedCombatModule.cs:416-422 — 6 lines twice; leave it if you would rather not add a shared static for 6 lines.
- **risk**: None for the deletions (all verified zero-caller). The ModuleDescription fix is a string change with no behavioural effect.

## Assets/Game/Scripts/Agents/AI/Motors/RigidbodyMotor.cs:41-70,72-101,152-175,224-238; FlyingRigidbodyMotor.cs:47-79,81-103,181-207,258-275; HoverRigidbodyMotor.cs:65-91,93-129,189-197,162-175  [-9 / +1 for the comment collapse; -105 / +60 (net -45) for the full base class, which I do not recommend]
- **smell**: The three Rigidbody motors carry the same rider-latch + IMovementMotor scaffold three times, including a ~10-line explanatory comment that is copy-pasted almost verbatim in RigidbodyMotor.ApplyRiderInput and FlyingRigidbodyMotor.ApplyRiderInput (HoverRigidbodyMotor.cs:17 even points at it: "see FlyingRigidbodyMotor.ApplyRiderInput for the long-form version of that reasoning"). Identical or near-identical each time: the currentDestination/stopDistance/pendingRiderInput/hasPendingRiderInput/riderDriveFrame fields, Velocity, IsImmobile, HasReachedDestination, CurrentDestination, ApplyRiderInput (identical 5-line body), FixedUpdate (identical body), ForceStop, and FaceDirection.
- **change**: I am NOT recommending this one yet, and stating the numbers so the orchestrator can decide: an abstract `RigidbodyMotorBase` would delete roughly 105 duplicated lines across the three files and add roughly 60 (new file + base plumbing), for a net of about -45 — well under the bar the brief sets for a new abstraction, and it would put a new type between three files that are otherwise easy to read in isolation. The cheap subset is worth doing on its own: collapse the two near-verbatim 10-line ApplyRiderInput comment blocks to one canonical version plus a one-line cross-reference (-9 / +1), which is what HoverRigidbodyMotor already does. If FlyingRigidbodyMotor is removed (see removals) the duplication drops from three copies to two and this becomes moot.
- **risk**: The comment collapse is zero-risk. The base class is medium risk for little gain — it touches three motors on live prefabs (Driller, Drone_truck, Drone_Saw_Full, ShipRV, Ship) and reorders no logic but does move serialized fields into a base type, which Unity handles but which makes the prefab YAML diff large and hard to review.

## Assets/Game/Scripts/Weapons/Core/Weapon.cs:47-53,89-93,108-111,117-123,375-404 and Assets/Game/Scripts/Weapons/Core/Magazine.cs:18-25,66-114  [-70 / +0]
- **smell**: Entire ammo/fire-rate event surface has zero consumers — there is no ammo UI in the project. `grep -rn '\.CurrentAmmo|\.MaxAmmo|\.AmmoPercentage|\.Magazine\b|OnAmmoChanged|\.Refill\(|OnAmmoEmpty|OnAmmoFull|SetAmmo|AddAmmo|FireRatePercent|OnFireRateReady' Assets --include=*.cs` finds callers only inside these two files. Weapon.Update's OnFireRateReady trigger is additionally unreachable (see bugs).
- **change**: Delete Weapon.OnAmmoChanged, OnFireRateReady, CurrentAmmo, MaxAmmo, Magazine, FireRatePercent, AddAmmo, Refill, OnMagazineAmmoChanged, the magazine.OnAmmoChanged subscribe/unsubscribe pair, and the dead `if (Time.time >= nextFireTime && !IsReadyToFire)` block; delete Magazine.OnAmmoEmpty, OnAmmoFull, AmmoPercentage, IsFull, AddAmmo, SetAmmo and their invocations. Keep ConsumeAmmo, Refill (called from Weapon.OnEnable), IsEmpty, CurrentAmmo/MaxAmmo on Magazine only if you keep the OnEnable refill.
- **risk**: Low — pure deletion of unreferenced public members inside Assembly-CSharp. Anything a future ammo HUD needs can be re-added when the HUD exists.

## Assets/Game/Scripts/Items/Inventory/Core/Inventory.cs:107-134,153-157  [-33 / +0]
- **smell**: Dead pair plus a formatting defect: TryMoveItem has no callers anywhere (`grep -rn 'TryMoveItem|SwapItems' Assets --include=*.cs` matches only these lines), SwapItems is called only from TryMoveItem, and line 124 reads `successfulMove = SwapItems(to,from);;` (double semicolon, and the arguments are in (to,from) order against a (indexA,indexB) signature). Brace style also switches mid-method.
- **change**: Delete both methods. Drag-and-drop in DevInventoryUI does not use them.
- **risk**: Low. If a future inventory-drag feature needs a move, it will want the OnSlotChanged-raising version anyway, which this is not quite.

## Assets/Game/Scripts/Weapons/Firearms/BasicGun.cs:14-15,57-65 and Assets/Game/Scripts/Weapons/BallLightning/BallLightningWeapon.cs:16,96-104  [-20 / +1]
- **smell**: Copy-pasted `private new Vector3 GetSpawnPosition()` in both subclasses hiding the base's `protected virtual` one, each backed by a `projectileSpawnPoint` field that duplicates Weapon.firePoint. On Gun.prefab projectileSpawnPoint and firePoint are literally the same object (both fileID 6642809435290787583); on CixinGunFinal both are unset.
- **change**: Delete both `projectileSpawnPoint` fields and both GetSpawnPosition hides; change Weapon.GetSpawnPosition to `Transform o = firePoint != null ? firePoint : transform; return o.position + o.forward * spawnOffset;`
- **risk**: Low but check the fallback: the base currently falls back to aimCamera.transform, the subclass versions fall back to `transform`. Using `transform` preserves today's behaviour for both live prefabs (Gun has firePoint set; CixinGunFinal has neither set and today spawns at the gun).

## Assets/Game/Scripts/Items/Artifacts/Leash/Leash.cs:68,282,294 + LeashAttachable.cs:49 + LeashArtifact.cs:62-64,70,81,87,91,96,103,112,118,135,140 + Prefabs/Items/Artifacts/Gadgets/Leash.prefab:72  [-22 / +0]
- **smell**: Debug scaffolding shipped switched ON. Leash.cs logs on Awake, on OnDestroy, and dumps a full `System.Environment.StackTrace` on every Dispose; LeashAttachable logs unconditionally in OnDestroy; LeashArtifact has 11 `if (debugLogs) Debug.Log(...)` calls in the click path with `debugLogs = true` as the field default AND `debugLogs: 1` serialized on the shipped prefab, so every left-click with the Leash equipped writes 3-6 console lines.
- **change**: Delete the three Leash.cs Debug.Log calls (the StackTrace one especially — it string-formats a stack trace on every dispose), the LeashAttachable.OnDestroy log, and the LeashArtifact debugLogs field with its 11 guarded calls; keep the two genuine Debug.LogWarning diagnostics. Set Leash.prefab debugLogs to 0 if the field is kept instead.
- **risk**: Low. Loses the leash-lifecycle trail the comments say was used to chase an endpoint-destruction bug — that bug is real and still open (see bugs), so fix the leak in the same pass.

## Assets/Game/Prefabs/Items/Artifacts/Gadgets/RuinScanner.prefab:123,190 and Assets/Game/Prefabs/Characters/Player/PlayerCharacter.prefab:443  [-0 / +0 (3 prefab values)]
- **smell**: Three more debug toggles left on in shipped prefabs: RuinScannerArtifact.debugLogAim = 1 (logs source/aim/origin/muzzle on every pulse), HoldAnimator.debugLog = 1 on the same prefab (logs every Hold transition while held), BackpackController.verboseDeploy = 1 (logs every deploy). Lasso.prefab and GrapplingHook.prefab correctly have debugLog: 0.
- **change**: Flip the three serialized values to 0. Data-only change, no code.
- **risk**: None.

## Assets/Game/Scripts/Weapons/Projectiles/BasicProjectile.cs:71-80  [-10 / +0]
- **smell**: `Debug.Log($"BasicProjectile hit {hitCollider.gameObject.name} at {position}")` in an override whose only content is that log — a per-impact console write in shipping code.
- **change**: Delete the whole OnImpact override (the base implementation is already an empty extension point).
- **risk**: None.

## Assets/Game/Scripts/Items/Artifacts/Gadgets/GrapplingHookArtifact.cs:180-198, Assets/Game/Scripts/Items/Artifacts/Gadgets/LassoArtifact.cs:412-442, Assets/Game/Scripts/Items/Artifacts/Leash/Leash.cs:169-192  [-74 / +37 (net -37, adds one file)]
- **smell**: The same rope-into-LineRenderer draw is written three times: set positionCount, loop t = i/(segments-1), Lerp(start,end,t), `pos.y -= Mathf.Sin(t * Mathf.PI) * sag`, SetPosition. Lasso adds a wobble term, Leash computes sag from slack, GrapplingHook scales sag by span/maxRange — all variations on the same 6 lines. GrapplingHook and Lasso also each carry an identical `GetRopeStart() => muzzle != null ? muzzle.position : owner.transform.position`.
- **change**: One static helper (e.g. Items/Artifacts/RopeLine.cs) `Draw(LineRenderer line, Vector3 start, Vector3 end, int segments, float sag, float headProgress = 1f, Vector3 wobbleAxis = default, float wobbleAmp = 0f, float wobbleFreq = 0f, float wobbleTime = 0f)`; the three call sites collapse to 1-3 lines each.
- **risk**: Medium-low, and it is the one item here that adds a file — only worth doing because it deletes about twice what it adds. The rope visuals are pure cosmetics, so a regression is visible immediately rather than silent.

## Assets/Game/Scripts/Items/Inventory/Components/EquipmentNetworkSync.cs:37  [-2 / +0]
- **smell**: `var item = Registry<InventoryItem>.Get(itemID);` inside EquipServerRpc is never used (the RPC just forwards the id to EquipClientRpc).
- **change**: Delete the line.
- **risk**: None.

## Assets/Game/Scripts/Items/Core/PickupableItem.cs:2  [-1 / +0]
- **smell**: `using Unity.VisualScripting;` — nothing in the file resolves through that namespace (NetworkBehaviour, IInteractable, Network, InventoryItem, IPlayerInventory, BackpackController, GameServices are all accounted for by the other four usings). Also the whole file is indented with 3 spaces against the project's 4, and the class is declared `class` rather than `public class` unlike its neighbours.
- **change**: Drop the using; reindent to 4 spaces; make the class public for consistency.
- **risk**: None (the using would fail to compile away only if an extension method were in play; none is).

## Assets/Game/Scripts/Weapons/Projectiles/Projectile.cs:123-130  [-8 / +0]
- **smell**: `RestartLifetime()` is a virtual wrapper whose entire body is `StartLifetime();`, with one caller (BallLightningProjectile.LaunchCharged).
- **change**: Delete it and call StartLifetime() directly.
- **risk**: None.

## Assets/Game/Scripts/Weapons/Firearms/BasicGun.cs:19  [-0 / +1]
- **smell**: `private void OnEnable()` hides `Weapon.OnEnable()` (protected virtual) without `override` or a `base.OnEnable()` call — so the base's magazine resolve/refill, event subscribe, nextFireTime reset and handle warning never run for BasicGun. BallLightningWeapon.cs:21 has the same shape but does call base.OnEnable(), which is the tell that this was an oversight. On today's Gun.prefab it is harmless (the Magazine is serialized and self-initialises in Awake, the camera resolves lazily, nextFireTime defaults to 0), so it is a latent trap rather than a live bug.
- **change**: `protected override void OnEnable() { base.OnEnable(); ... }` in BasicGun; add `override` to BallLightningWeapon's too and drop the CS0114 warning.
- **risk**: Low; it makes the base OnEnable actually run for BasicGun, which only adds a Refill and an event subscription.

## Assets/Game/Scripts/Items/Artifacts/RuinScanner/RuinScannerArtifact.cs:53-73  [-0 / +0 (reorder)]
- **smell**: Field declarations are interleaved with methods: `nextUseTime` + `CanUse()` sit between the [Header("Debug")] block and the [Header("VFX")]/[Header("Audio")] blocks, so the inspector order and the file order disagree with each other.
- **change**: Move CanUse() and nextUseTime below all serialized fields.
- **risk**: None.

## Assets/Game/Scripts/Weapons/BallLightning/BallLightningController.cs:16-23 + Prefabs/Items/Artifacts/ArtifactResources/BallLightningProjectile.prefab:300-307  [-25 / +0]
- **smell**: Dead toggles. On the only prefab that uses it, BallLightningBoltTargeting is enabled and calls SetExternalDirectBolt every Update, which latches `useExternalDirectBolt` permanently true — so randomizeDirectBoltActivation/randomizeDirectBoltPosition/directBoltUv/directBoltUvMin/Max/activeDurationRange/inactiveDurationRange/forceDirectBoltOn can never affect the output. The serialized values are also nonsense against the code: directBoltUvMin/Max are ±60 while LateUpdate Clamp01s the result, so under 1% of the random range is even representable.
- **change**: Either delete the internal randomiser (fields + the LateUpdate branch, ~25 lines) and keep only the external-bolt path, or keep it and fix the prefab's UV ranges to 0..1. Deleting is the net-negative option.
- **risk**: Low-medium: the randomiser is the only animation source if BallLightningBoltTargeting is ever removed from the prefab, and BallLightningController is [ExecuteAlways] so the editor preview would go static.

## Assets/Game/Editor/Agents/EntityProfileEditors.cs:27-67, Vehicles/DesertCrawlerBuilder.cs:470-501, Vehicles/OrnithopterBuilder.cs:323-366, Vehicles/WingPackBuilder.cs:174-199, Creatures/CrabWalkerBuilder.cs:290-321, Creatures/HorseBuilder.cs:340-371  [-166 / +6 (net -160)]
- **smell**: Copy-pasted SerializedProperty setter family (SetFloat/SetInt/SetBool/SetString/SetObject/SetLayerMask/Find) duplicated verbatim in six editor files, 207 lines total. A public static `EntityProfileEditorUtils` in EntityProfileEditors.cs already holds the canonical copy, and all six files share namespace SpaceGame.EditorTools in the same assembly (Assembly-CSharp-Editor — neither Assets/Game/Editor nor its subfolders carry an asmdef, verified).
- **change**: Delete the five duplicate blocks in the builders and call EntityProfileEditorUtils.Set*(...). Add the builders' one extra behaviour — the 'field was renamed, this value is unset' Debug.LogWarning from WingPackBuilder.Find — to EntityProfileEditorUtils as a ~6-line Find helper, so the diagnostic is not lost. No new file, no new namespace.
- **risk**: Low. Pure editor code, no serialized data. The builders' local Set* overloads take (SerializedObject, string, value) while EntityProfileEditorUtils takes (Object target, string, value) and constructs its own SerializedObject — so builders that batch many writes into ONE SerializedObject and call ApplyModifiedPropertiesWithoutUndo once (WingPackBuilder:56-66) would become one SerializedObject per field. Verify no builder relies on batching before/after semantics; if any does, keep a (SerializedObject, ...) overload set in the shared util instead.

## Assets/Game/Editor/World/WorldChunkerEditor.cs:73-160 and :1237-1299  [-63 (drop window grid) or -96 (drop SceneView grid) / +0]
- **smell**: Two independent implementations of the same interaction. OnSceneGUI (88 lines) draws a clickable chunk grid as Handles quads in the SceneView with click-toggle and shift-range-select; DrawSelectableGrid (63 lines) draws the same clickable chunk grid as EditorGUI.DrawRect cells in the window with click-toggle, shift-range-select and right-click-remove. Both mutate the same `selectedChunks` HashSet and the same `lastClickedCoord`, and the shift-range block is character-for-character identical (lines 136-142 vs 1276-1283).
- **change**: Keep one. The SceneView version is the useful one (you pick chunks on the actual terrain); the window grid is a redundant 8x6 abstract checkerboard. Delete DrawSelectableGrid and its call site, or delete OnSceneGUI plus the OnEnable/OnDisable duringSceneGui hooks (lines 63-71) if the abstract grid is preferred.
- **risk**: Low — pure editor UI, no serialized state. Ask the user which grid they actually use; deleting the wrong one removes a workflow rather than a duplicate.

## Assets/Game/Editor/Map/MapTileBaker.cs:327, Assets/Game/Editor/Map/MapMeshBaker.cs:171, Assets/Game/Editor/Vehicles/DuneFoilBuilder.cs:1173  [-20 / +0]
- **smell**: Three copies of the same recursive AssetDatabase folder-creation helper — two named CreateFolderRecursive with identical bodies, one named EnsureFolder with the same logic. ~10 lines each.
- **change**: Keep one (DuneFoilBuilder's EnsureFolder is the tightest) as a public static on the shared EntityProfileEditorUtils; delete the other two. Same assembly and namespace, so no using changes needed.
- **risk**: Very low. Note the shared version must stay case-correct — see the EnsureFolder bug below.

## Assets/Game/Editor/Agents/BehaviourModuleEditor.cs:1,81 and Assets/Game/Editor/Agents/EntityProfileEditors.cs:5,+matching #endif  [-4 / +0]
- **smell**: `#if UNITY_EDITOR` / `#endif` guards on files that live inside Assets/Game/Editor/, where UNITY_EDITOR is unconditionally defined. Dead scaffolding — and in BehaviourModuleEditor.cs the #endif is misplaced (see bug below).
- **change**: Delete both directive pairs.
- **risk**: None. Removing the guard also removes the brace-mismatch hazard.

## Assets/Game/Tests/EditMode/{CrabLocomotionTests,HorseLocomotionTests,HumanoidLocomotionTests,LateralTravelTests,OstrichLocomotionTests,SyntheticMachineTests}.cs  [cube helper: -20 / +6 (net -14). Full Trace/March consolidation would be roughly -200 / +80 but is only worth it if the six field sets really unify — measure before committing to it.]
- **smell**: Six near-duplicate copies of the same test harness: a private `struct Trace` (5-12 lines each) plus a `Trace March(...)` step-loop (28-58 lines each) = 265 lines, and five copies of the identical four-line cube-mesh trick (`var temp = GameObject.CreatePrimitive(PrimitiveType.Cube); cube = temp.GetComponent<MeshFilter>().sharedMesh; Object.DestroyImmediate(temp);`). A shared harness file already exists and is already used: WalkerTestRig.cs (102 lines, referenced by WalkerLegPoseTests, WalkerLegSolverTests, WalkerPlanarChainTests).
- **change**: Move the four-line cube-mesh helper and the common ground-slab setup into WalkerTestRig (existing file, no new file). Leave the Trace structs alone unless a superset genuinely fits — the field sets differ per machine (the ostrich Trace has FallingFrames and HighestPlantedFoot; the crab's does not), so forcing a common Trace would add fields nobody asserts on.
- **risk**: Medium for the full consolidation: these are the regression tests for six locomotion rigs, and a shared March() that quietly changes the step order or the SnapToGround timing changes what the assertions measure. Do the cube helper (safe) and stop there unless the user wants more.

## Assets/Game/Scripts/Presentation/UI/Pages/MinigameConfigUI.cs:199,300,424,440,447,454,465 + MatchResultUI.cs:31-33,217-230,232-238 + MatchLeaderboardUI.cs:24-26,250-260,263-269 + LoadingScreenUI.cs:30-32,259-272,274-280  [-129 / +7]
- **smell**: Two generations of code-built menus. UITheme/UIBuilder are used by exactly 4 files (DevInventoryUI 36/49 call sites, PauseMenuUI 31/54, SettingsWidgets 30/55, PlayerListView 12/16); MinigameConfigUI, MatchResultUI, MatchLeaderboardUI and LoadingScreenUI use NEITHER and each carry private clones of the same primitives. UIBuilder's own class comment even cites 'the bug MinigameConfigUI works around with an extra Content child per row' — it was written knowing about that file and the file was never migrated. Colors are copied verbatim: Accent (0.239,0.549,0.949,1) and Muted (0.62,0.70,0.82,1) are byte-identical to UITheme.Accent/UITheme.Muted in all three of MatchResultUI/MatchLeaderboardUI/LoadingScreenUI. MatchResultUI:30's comment 'Shared with MinigameConfigUI so the two minigame screens read as one set' is stale — MinigameConfigUI actually declares Bright/MenuNavy/Dim with different values.
- **change**: Delete the clones and call the existing shared helpers. CreateChild x3 (7 lines each) + NewRect (6) -> UIBuilder.Rect. NewLabel x3 (14+14+15) + the inline TMP block in MatchLeaderboardUI.Column:250-260 (11) -> UIBuilder.Label. The 3 color trios (9 lines) -> UITheme. MinigameConfigUI.EnsureEventSystem:465-469 (5) -> UIBuilder.EnsureEventSystem (keep MatchResultUI:243-250, whose no-DontDestroyOnLoad behaviour is deliberate and documented at :247, or add a bool param). AddLayoutHeight/AddLayoutWidth:447-460 (14) -> UIBuilder.FixedHeight plus one new 7-line UIBuilder.FixedWidth. AddSpacer:300-304 (5) -> UIBuilder.Spacer. TintText:199-213 (15) -> UIBuilder.Clickable.
- **risk**: Callsite churn: CreateChild returns GameObject with `out RectTransform` whereas UIBuilder.Rect returns RectTransform, so each call becomes `var r = UIBuilder.Rect(n,p);` and later `r.gameObject`. Mechanical but touches ~40 call sites across 4 files, all visually verifiable by opening each screen. UIBuilder.Label sets extra properties (enableWordWrapping=false, overflowMode=Ellipsis) that the clones do not, so long labels will ellipsize instead of wrapping — check the MatchResultUI subtitle and the MinigameConfigUI summary line.

## Assets/Game/Scripts/Presentation/UI/Pages/MinigameConfigUI.cs:219-231, MatchResultUI.cs:150-159, MatchLeaderboardUI.cs:151-163, LoadingScreenUI.cs:214-224, PauseMenuUI.cs:250-261, DevInventoryUI.cs:303-314, UI/Map/MapPOIController.cs:152-161  [-56 / +14]
- **smell**: The same Canvas + CanvasScaler(ScaleWithScreenSize, 1920x1080) + GraphicRaycaster construction block is written out seven times, ~10 lines each. UIBuilder has helpers for every other primitive but not for the overlay root, which is why every screen hand-rolls it.
- **change**: Add one UIBuilder.Overlay(string name, Transform parent, int sortingOrder, bool raycaster = true, bool canvasGroup = false) returning the root RectTransform, and replace the seven blocks with one call each. Only sortingOrder and the raycaster/CanvasGroup presence actually differ between them (900/1000/1100/2000/2100/5000/1000).
- **risk**: MapPOIController is on the removal list, so that call site may vanish first (drops the win to -46). PauseMenuUI and DevInventoryUI additionally set matchWidthOrHeight=0.5f which the others leave default — either pass it or set it on the returned canvas at those two sites.

## Assets/Game/Scripts/Presentation/UI/HelmetHUD/HelmetNavMarkers.cs:80,82,84,86,88,210-215,216-231,245,439-459 + HelmetMarkerFactory.cs:29,105-140  [-55 / 0]
- **smell**: Debug scaffolding left in a shipping HUD component, and it is provably unreachable rather than merely off by default. HelmetHUDController.cs:107 does `navMarkers = go.AddComponent<HelmetNavMarkers>()` inside `if (navMarkers == null)`, and PlayerHUD.prefab line 988 has `navMarkers: {fileID: 0}` — so the component is ALWAYS created at runtime and every [SerializeField] takes its C# initializer, with no inspector able to change it. That fixes debugLog=false, debugForceDefaultMaterial=false, debugSelfTestMarkers=false and showEvenWithoutPlayerFaction=true forever.
- **change**: Delete the four toggles and everything they gate: the per-second Debug.Log block at 210-215 plus the nextDebugLog field; the self-test marker block at 216-231 plus EnsureSelfTestTransforms at 439-459 plus the selfTestTransforms list; the always-false `continue` at line 245; and the useDefaultMaterial parameter on HelmetMarkerFactory.Build (signature at HelmetMarkerFactory.cs:29) together with its branch, since debugForceDefaultMaterial is its only caller (HelmetNavMarkers.cs:150).
- **risk**: Loses a real diagnostic used when the helmet HUD renders nothing (the self-test spawns 4 markers at +/-10m N/E/S/W to prove rendering works independently of factions/POIs). Given it can no longer be switched on without editing code, deleting it and re-adding a temporary block when needed is equivalent. If you would rather keep it, the honest fix is to wire navMarkers in PlayerHUD.prefab so the toggles become reachable — but that is +0/-0 and leaves the dead-toggle smell.

## Assets/Game/Scripts/Presentation/UI/HelmetHUD/HelmetNavMarkers.cs:235-238  [-4 / 0]
- **smell**: An if statement with an empty body: `if (playerFaction == null && !showEvenWithoutPlayerFaction) { /* No player faction yet, and configured to suppress unrelated entities. */ }`. The condition is evaluated and nothing happens, then execution falls into the entity loop regardless. Reads like a `return` was intended.
- **change**: Delete the block. I verified this is NOT a bug: the identical condition is correctly applied per-entity as `continue` at line 245, so the suppression behaviour is already implemented one scope down.
- **risk**: None. Confirm you delete 235-238 and not the load-bearing guard at 245.

## Assets/Game/Scripts/Presentation/UI/Pages/PauseMenuUI.cs:150-159 and UI/Pages/DevInventoryUI.cs:138-147  [-10 / 0]
- **smell**: IsTypingInField() is duplicated verbatim, 10 lines each, in the two screens that own a keyboard shortcut which is also a letter (M and I).
- **change**: Move one copy to UIBuilder (or a small static next to it) as a public static bool, delete the other. Both call sites already reference UIBuilder.
- **risk**: None; the two copies are character-for-character identical.

## Assets/Game/Scripts/Presentation/Audio/AudioManager.cs:20-23,106-109,119-122 + UI/Widgets/SettingsWidgets.cs:49-56 + UI/LobbyMenu/Widgets/LobbyUIManager.cs:47-50  [-24 / 0]
- **smell**: Dead public API kept alive by nothing. AudioManager.PlayTestMusic() hardcodes 'event:/Music/TestSong' and has 0 callers (also absent from every .prefab/.unity/.asset, so not UnityEvent-bound); the SfxVolume/MusicVolume/ReverbVolume/UIVolume properties have 0 external callers because ApplySettings writes the backing fields directly from GameSettings; PlaySFX(string) has 0 callers (Weapon.cs:413,424 use PlaySFX3d). SettingsWidgets.Divider() has 0 callers. LobbyUIManager.refreshLobbyList() is an empty private method with 0 callers. AudioManager also carries two '//Todo:' trailing comments at :109 and :122.
- **change**: Delete PlayTestMusic (4), the four volume properties (4), PlaySFX(string) (4), SettingsWidgets.Divider (8), LobbyUIManager.refreshLobbyList (4), and the two stale Todo comments.
- **risk**: Low. Divider is a plausible future need for the settings pages but is trivially re-derivable from the adjacent Caption/Heading builders. Verify once more that no FMOD-side tooling calls PlayTestMusic before deleting.

## Assets/Game/Scripts/Presentation/UI/HUD/DeathScreenUI.cs:25, UI/Map/MapHologramTerrain.cs:782, UI/Pages/MinigameConfigUI.cs:115  [-4 / 0]
- **smell**: Debug.Log left on shipping code paths. DeathScreenUI:25 logs the unprefixed 'Show death screen invoked' on every player death (every other log in Presentation uses a [Tag] prefix). MapHologramTerrain:782 logs chunk-mesh counts on every startup. MinigameConfigUI:115 logs on every match start.
- **change**: Delete the DeathScreenUI line outright; downgrade the other two to Debug.Log guarded by an existing verbosity flag or delete them. The fourth instance, StartingGameManager.cs:34 `Debug.Log(networkManager.SceneManager)` (a raw object dump), goes away with that file's removal.
- **risk**: None. MapHologramTerrain:782 is mildly useful while the missing-mesh gap in bug 3 is unresolved — fix that first, then drop the log.

## Assets/Game/Scripts/Presentation/UI/Map/MapHologramTerrain.cs (whole file, 1345 loc)  [0 / 0 (deliberately no change proposed)]
- **smell**: Genuine god class: 33 methods and 73 [SerializeField] fields on one MonoBehaviour, spanning baked-mesh loading, procedural mesh generation (BuildDiamondMesh/BuildBoxMesh/BuildRingMesh/BuildSunrayFanMesh), material creation, marker + label management, discovery/fog sampling, hologram-layer management, container inverse-scale math, beam transform and shader uniforms. 73 tunables in one inspector is unusable in practice.
- **change**: NOT RECOMMENDED under the net-negative rule — any split adds files and adds lines. Recording it as known structural debt instead. I checked for the cheap win and there is none: a per-field usage scan found 0 never-read serialized fields (all 73 are read), so there is nothing to delete. If it is ever split, the clean seam is the three pure static mesh builders at 629-661, 662-689 and 690-722 (94 lines, no instance state), which move without behavioural risk — but only once something else needs them.
- **risk**: Leaving it as-is costs nothing at runtime; it is a maintainability cost only. Do not let a future cleanup pass split it 'for tidiness' without a second consumer to justify the added files.

## Assets/Game/Scripts/Presentation/UI/Map/MapService.cs:116-131,166-171  [-22 / 0 in MapService, up to -43 including the MapHologramTerrain handler]
- **smell**: Cascade from the dead-2D-map removals, not an independent smell. RegisterMarker() and RemoveMarker() have exactly two callers between them — MapMarker.cs and MapPOIController.cs — both on the removal list. AddStaticMarker survives because RegisterPOI (live via MapPOI) calls it internally at :157/:159.
- **change**: After removing MapMarker.cs and MapPOIController.cs, delete MapService.RegisterMarker (16 lines) and RemoveMarker (6 lines). Note this leaves OnMarkerRemoved永 never raised, so MapHologramTerrain's OnMarkerRemoved subscription (SubscribeToMapService:762) and its OnMarkerRemoved handler at :872 become dead too (~21 more lines).
- **risk**: Medium — do this only AFTER the 2D map removal is committed, and only if you accept that markers become add-only for the session. If you might revive dynamic markers (an enemy that dies should drop off the map), keep RemoveMarker. I would keep it; the 22 lines are not worth losing the capability.

## ProjectSettings/GraphicsSettings.asset (m_AlwaysIncludedShaders)  [-0 / +7 (7 YAML list entries)]
- **smell**: 7 project shaders are only obtainable via Shader.Find() at runtime, are in no material, and are not in Always Included Shaders — so Unity strips them from player builds
- **change**: Add the 7 shader GUIDs (HologramTerrain, HologramBeam, HologramSolid, MapHolographic, HelmetHUDHolographicUI, HelmetHUDHologramSolid, HelmetHUDDangerVignette) to m_AlwaysIncludedShaders. This is the minimal fix; the alternative (authoring 7 material assets and wiring them into the prefabs) adds far more.
- **risk**: Adds ~7 shader variants to build size. Zero runtime risk. See the matching bug entry for the failure it fixes.

## ProjectSettings/EditorBuildSettings.asset  [-8 entries / +0]
- **smell**: 263 scene entries of which 2 point at files that do not exist and 6 are developer scratch/test scenes shipped in the player build
- **change**: Delete the 2 dangling entries (Assets/Game/Scenes/Interiors/TestInterior.unity, Assets/Game/Scenes/Interiors/InsideRuin.unity — neither on disk nor in git ls-files) and set enabled: 0 on the 6 test entries (Blocking test, Aleksander/Tommy/Emil/Marius test scene, Ferdinand_Test_world). Leaves 7 real entries + 248 chunks.
- **risk**: Any code doing SceneManager.LoadScene(buildIndex) shifts. Verified only one such call: Scripts/Core/SceneManagement/Core/Bootstrapper.cs:21 SceneManager.LoadScene(0) — index 0 is Bootstrap.unity and stays index 0 because the dangling entries sit at positions 2 and 12. Everything else loads by name.

## Assets/Game/Art/Materials/Untitled/ (4 files)  [-1 file, -1 folder / +0 (renames only)]
- **smell**: A folder literally named Untitled holding 4 default-named 'New Material' assets — but 3 of them are LIVE production materials, so this cannot simply be deleted
- **change**: DELIVERABLE ANSWER: 'New Material.mat' (pure white, metallic 0) is refcount 0 -> delete. 'New Material 1.mat' (grey 0.354, metallic 0.2), 'New Material 2.mat' (black, metallic 0.2) and 'New Material 3.mat' (bone 0.915/0.861/0.777, metallic 0) are each referenced by CowBotRocket.prefab, Vehicles/Rover.prefab and Vehicles/RoverNoHierarchy.prefab (New Material 2 additionally by Art/Materials/Surfaces/Custom_Wood.mat). Rename them to RoverGrey.mat / RoverBlack.mat / RoverBone.mat, move all three to Art/Materials/Surfaces/, then delete the Untitled folder. Renaming/moving a .mat is GUID-safe (GUID lives in the .meta) so no prefab breaks. Same treatment for the two other default-named survivors: Art/Materials/Settlement/New Material.mat (1 ref) and Settlement/New Material 1.mat (6 refs).
- **risk**: The .meta MUST travel with each .mat or all 3 prefabs lose their materials. Do it through the Unity Editor, not the shell.

## .gitignore  [-2.69 MB tracked / +2 gitignore lines]
- **smell**: Assets/StreamingAssets/*.bank are FMOD-generated copies of Assets/Game/Audio/*.bank yet both are committed (2.69 MB duplicated in every clone and every future bank rebuild doubles the diff)
- **change**: Either (preferred) move the FMOD source bank folder out of Assets/ and repoint Assets/Plugins/FMOD/Resources/FMODStudioSettings.asset sourceBankPath, or add '/Assets/StreamingAssets/*.bank' + '.meta' to .gitignore since FMOD regenerates them.
- **risk**: Gitignoring StreamingAssets banks means a fresh clone has no banks until Unity+FMOD refresh runs — CI builds would need an explicit FMOD refresh step. Moving the source out of Assets is the cleaner option.

## Assets/Game/Prefabs/ — three overlapping roots: Prefabs/Vehicles/, Prefabs/Agents/Vehicles/, Prefabs/Legacy/Vehicles/; and two lighting roots: Prefabs/Environment/Sky/ vs Prefabs/VisualEffects/Lighting/  [-3 folders, -11 prefab files / +0]
- **smell**: The restructure created new domain folders but copied instead of moved, so the live prefab and its dead twin now live in different trees and a reader cannot tell which is real
- **change**: Delete the dead twins (listed in removals), then MOVE the surviving live prefab into the new location: Prefabs/Vehicles/Ship.prefab -> Prefabs/Agents/Vehicles/Spacecraft/Ship.prefab, Prefabs/Vehicles/Rover*.prefab -> Prefabs/Agents/Vehicles/Ground/. Keep Prefabs/Environment/Sky/ as the single lighting/atmosphere root and drop Prefabs/VisualEffects/Lighting/ entirely. Result: one vehicle root, one lighting root, Prefabs/Vehicles/ and Prefabs/Legacy/ both gone.
- **risk**: Moves must carry the .meta so the GUID survives; persistentScene, Blocking test, Ferdinand_Test_world, Markus Music Test Scene and both DefaultNetworkPrefabs lists all reference Prefabs/Vehicles/Ship.prefab by GUID and will follow the move silently. Do it in the Editor.

## Assets/Game/Prefabs/Environment/Nature/Plants/, Prefabs/Items/Debug/ + Prefabs/VisualEffects/Debug/, Prefabs/VisualEffects/CutsceneExamples/  [-4 folders / +1 folder]
- **smell**: Category folders whose contents contradict their names (2 barrels in Plants), debug scaffolding split across two trees, and an 'Examples' folder inside a VisualEffects tree
- **change**: After the removals above, Plants/ retains only Beavertail + Candelabre-thorns + Cactus (all three die with MasterScene) so the folder disappears. Collapse Prefabs/Items/Debug/ and Prefabs/VisualEffects/Debug/ into one Prefabs/_Debug/ (only Items/Debug/Cube+Sphere survive, both live via Resources/Items/Debug/*.asset). CutsceneExamples/ folder disappears with its contents.
- **risk**: Low; Cube/Sphere prefabs are in DefaultNetworkPrefabs by GUID so the move is transparent.

## Assets/Game/Scripts/Gameplay/Interaction/Triggers/VolumeTrigger.cs:45,87-109,111-158  [-36 / +0]
- **smell**: Debug scaffolding left in shipping code, running in the streamed world. OnTriggerEnter (line 87) and OnTriggerExit (line 93) Debug.Log unconditionally on every overlap; OnTriggerStay logs once a second per collider; and the whole `string source` parameter threaded through TryFire exists only so those logs can be gated to "Enter". This component is placed in Chunk_7_5.unity and FerdinandChunk_3_1.unity, so it logs during normal play.
- **change**: Delete OnTriggerExit entirely (it does nothing but log), delete the `lastStayLog` field, delete all six Debug.Log/LogWarning sites, and change `TryFire(Collider other, string source)` to `TryFire(Collider other)` — dropping the two call-site arguments and the five `if (source == "Enter")` guards. Keep the Debug.LogWarning in Awake about isTrigger; that one is a genuine authoring check.
- **risk**: Loses the interior round-trip diagnostics. Low — SceneTransition's own busy/lockout state is still inspectable, and the comments explaining each guard stay.

## Assets/Game/Scripts/Core/SceneManagement/Transitions/Core/SceneTransition.cs:33-42,92-116,136-146,150,187,189,202,208-209  [-44 / +0]
- **smell**: Same debug scaffolding, self-flagged. Line 136 says verbatim "Diagnostic so we can see WHY repeat triggers are being denied ... Remove once round-trip is solid." On top of that: a whole throttled-logging subsystem (`s_lastDeniedLog` dictionary + LogDeniedThrottled, lines 109-116) exists only to rate-limit those logs, and CanTrigger builds interpolated strings (including a dictionary lookup on line 103) on every denied frame. Separately, lines 33-42 are an 11-line [TextArea] `description` string field: inspector help text that is serialized into every prefab and scene instance (4 assets).
- **change**: Delete the `s_lastDeniedLog` dictionary and LogDeniedThrottled, the three LogDeniedThrottled calls in CanTrigger, the 8-line `reason` ladder plus Debug.Log in Trigger, and the four progress Debug.Logs at 150/187/189/202. Keep the self-heal warning in Update (68-73) — that one reports a real stuck state. Move the `description` text into the class XML doc (which already says the same thing) and delete the serialized field.
- **risk**: Low. The `description` field removal will show as a removed property on the 4 referencing assets; harmless serialized-data drop.

## Assets/Game/Scripts/Core/Multiplayer/NetworkGameManager.cs:62,87,107,111,149,155  [-6 / +0]
- **smell**: Six `[NGM DEBUG]` Debug.Log lines in the player-spawn path, including one that prints GetInstanceID(). The tag itself marks them as temporary.
- **change**: Delete all six. The surrounding comments already document the invariants they were added to observe.
- **risk**: None.

## Assets/Game/Scripts/Core/SceneManagement/Transitions/Destinations/InteriorSceneDestination.cs:41,46,57,60-64,77  [-11 / +0]
- **smell**: Four Debug.Logs plus a once-per-second 'still waiting' progress logger with its own `nextLog` timer variable, inside the interior-load wait loop.
- **change**: Delete the nextLog field and the 5-line progress block, and the three informational Debug.Logs at 41/46/77. Keep the Debug.LogError on timeout (67-72) — it names real causes.
- **risk**: None.

## Assets/Game/Scripts/Core/GameServices/Core/GameServices.cs:8-28 (+ deletes Gameplay/Game/State/Game.cs and GameMode.cs)  [-47 / +2 (2 files deleted)]
- **smell**: A game-mode switch and a mode-changed event that can never fire. Game.SetMode has zero callers repo-wide, so Game.Mode is always Singleplayer; `case GameMode.Multiplayer: break;` is unreachable dead code that would additionally leave ItemDropService and World null (and NRE in EntityLootTable.cs:64/78, EquipmentController.cs:169, PickupableItem.cs:53) if it ever were reached. LoadServices also ignores its own `mode` parameter and re-reads the static.
- **change**: Replace Initialize()/LoadServices with `public static void Initialize() { ItemDropService = new PlayerDropService(); World = new WorldService(); }`, then delete Game.cs and GameMode.cs.
- **risk**: Medium: it removes the seam for per-mode service swapping. Since the seam has never been exercised, re-adding it later costs the same 30 lines and would then actually be wired.

## Assets/Game/Scripts/Gameplay/Minigame/Core/MatchWinEvaluator.cs:31-59  [-16 / +4]
- **smell**: Copy-pasted logic. EvaluateLivesExhausted and EvaluateLastStanding are structurally identical — both count dictionary entries with value > 0 and return the single survivor's key or null. Only the parameter name and the doc comment differ.
- **change**: Keep one `private static int? SingleTeamWithValue(IReadOnlyDictionary<int,int> byTeam)` and make both public methods one-line delegations, preserving both names (MatchManager.cs:696-697 and Assets/Game/Tests/EditMode call them by name) and both doc comments.
- **risk**: None; covered by MatchRulesTests-adjacent EditMode tests.

## Assets/Game/Scripts/Core/Persistence/Runtime/WorldSaveStore.cs:53-54,77,87  [-8 / +1]
- **smell**: Dead event. `public event Action<string, Scene> OnSceneHydrated` is declared and raised twice but a repo-wide grep for OnSceneHydrated finds only the declaration and the two raise sites — zero subscribers anywhere in Assets/.
- **change**: Delete the event and both invocations; the early-return block at 75-79 then collapses to `if (!world.TryGet(sceneKey, out SceneRecord record)) return;`.
- **risk**: Low — it is an extension point nothing has used. Keep it if a chunk-hydrated hook is imminent.

## Assets/Game/Scripts/Core/Persistence/Format/ISaveable.cs:39-52, Runtime/SaveableEntity.cs:242-258, Runtime/PlayerSaveService.cs:88  [-33 / +0]
- **smell**: Unused extension point. `IDeferredSaveable` has ZERO implementors: a grep for IDeferredSaveable across all of Assets/ returns only its own declaration and the `is not IDeferredSaveable` test inside SaveableEntity.NotifyLoadComplete. So NotifyLoadComplete always iterates every saver and does nothing, and PlayerSaveService.cs:88 calls it on every player restore for no effect.
- **change**: Delete the IDeferredSaveable interface, SaveableEntity.NotifyLoadComplete(), and the PlayerSaveService call site.
- **risk**: Medium: this is a deliberately designed hook for savers that need the world settled (the doc comment is specific about the use case). If anyone is mid-way through implementing one, keep it. Otherwise it is 33 lines of aspiration.

## Assets/Game/Scripts/Core/Settings/GameSettings.cs:432-436,468-472  [-8 / +0]
- **smell**: Dead preprocessor branches. `#if UNITY_2022_2_OR_NEWER` / `#else` in ApplyScreen and RefreshHz — ProjectSettings/ProjectVersion.txt is 6000.3.11f1, so the #else arms (Resolution.refreshRate, removed API) can never compile in this project and are pure noise.
- **change**: Delete both #if/#else/#endif wrappers, keeping only the refreshRateRatio arms.
- **risk**: None unless the project is ever downgraded below 2022.2.

## Assets/Game/Scripts/Core/Settings/GameSettings.cs:27-29,346-349  [-8 / +0 (delete) or -0 / +2 (implement)]
- **smell**: Dead toggle with a misleading comment. SchemaVersion's comment says "Everything is re-seeded from defaults when the stored version is older", but EnsureLoaded only writes the new version number and never re-seeds anything. Bumping SchemaVersion to 2 would silently do nothing.
- **change**: Either delete the SchemaVersion constant and the 4-line block, or make the block actually call the reset path. Deleting is the net-negative option; if kept, the comment must change.
- **risk**: Low. If deleted, a future default change has no migration story — but it does not have one today either.

## Assets/Game/Scripts/Gameplay/Interaction/Core/Interactor.cs:1,3,6,204; Triggers/InteractableProxy.cs:1; Interactions/ShipInteraction.cs:2; Characters/Player/Equipment/Flashlight.cs:3; Characters/Player/Movement/SpectatorCamera.cs:3; Core/SceneManagement/Transitions/Core/TransitionRunner.cs:3; Core/SceneManagement/Interiors/InteriorScene.cs:4; Gameplay/Game/State/GameManager.cs:3; Gameplay/Minigame/Runtime/MatchSettings.cs:1-2  [-11 / +0]
- **smell**: Unused using directives, verified individually rather than assumed. Interactor.cs imports Unity.Netcode and SpaceGame.Agents and uses no type from either (0 hits for Netcode/NetworkBehaviour/NetworkObject/AgentController/AgentTargeting/EntityFaction in the body); it also writes `System.Array.Sort` fully-qualified on line 204 despite `using System;` on line 1. InteractableProxy and GameManager import System.Linq and use no LINQ. ShipInteraction imports System.Runtime.InteropServices. Flashlight, SpectatorCamera and TransitionRunner import SpaceGame.Presentation and reference Presentation types only inside comments. InteriorScene imports SpaceGame.World (inside the #if, and uses nothing from it). MatchSettings imports both SpaceGame.Core and SpaceGame.Presentation and uses neither.
- **change**: Delete the 11 directives; shorten Interactor.cs:204 to `Array.Sort(...)`.
- **risk**: None; compiler-verifiable.

## Assets/Game/Scripts/Core/Persistence/Runtime/SaveManager.cs:298-299; Gameplay/Game/Spawning/SpawnManager.cs:129-132,139; Gameplay/Interaction/Interactions/ShipInteraction.cs:16; Core/SceneManagement/Transitions/Effects/FadeToBlackEffect.cs:38-39,80,104; Gameplay/Interaction/Core/Interactor.cs:35-37  [-22 / +0]
- **smell**: Dead members and dead locals, each grep-verified. SaveManager.QuickSave() has no callers (SaveHotkeys.cs:52 calls manager.Save(...) directly). SpawnManager.SpawnNetworkPlayer() has no callers. SpawnManager.cs:139 assigns `NetworkObject netObj` and never reads it (the name appears exactly once in the file body). ShipInteraction's `[SerializeField] private Transform ship` is never read (the token `ship` appears only in the declaration and twice in a doc comment). FadeToBlackEffect's `outRoutine`/`inRoutine` are assigned twice each and never read. Interactor's hitNormalColor/hitInteractableColor/missColor are non-serialized private gizmo colours.
- **change**: Delete SaveManager.QuickSave, SpawnManager.SpawnNetworkPlayer, the unused netObj local, ShipInteraction's `ship` field, FadeToBlackEffect's two Coroutine fields and their four assignments, and inline Interactor's three gizmo colours as literals in OnDrawGizmos.
- **risk**: Removing ShipInteraction's `ship` field will drop a serialized reference on Ship.prefab — verify nothing in the inspector depended on it being visible. Everything else is private/uncalled.

## Assets/Game/Scripts/Gameplay/Interaction/Core/Interactor.cs:33-34,234-265  [-1 / +2 (or -35 / +0 if the gizmo is dropped outright)]
- **smell**: Debug gizmo on by default in shipping code: `[Header("Debug")] [SerializeField] private bool _debugRay = true;` plus a 32-line OnDrawGizmos, and the `rayCastHit`/`hitInfo` fields plus line 171 (`if (!found && hitInfo.collider == null) hitInfo = rayHits[0];`) exist purely to feed it.
- **change**: Flip the default to false at minimum; better, wrap OnDrawGizmos and the two fields in #if UNITY_EDITOR so they cost nothing in a build, and drop line 171's gizmo-only fallback assignment.
- **risk**: Low; it is a diagnostic aid the team may still want in the editor.

## Assets/Game/Scripts/Core/Persistence/Runtime/SaveableEntity.cs:13-35  [-0 / +0 (pure move)]
- **smell**: Two consecutive <summary> blocks on one declaration. The first block (13-26) documents the SaveableEntity CLASS but sits immediately above `public enum SaveScope`, and the second (27-35) documents SaveScope. The class at line 46 therefore has no doc comment at all, and IDE tooltips on SaveScope show the wrong text.
- **change**: Move the first <summary> block down to sit above `public class SaveableEntity`.
- **risk**: None.

## Assets/Game/Scripts/Gameplay/Health/NetworkedHealthComponent.cs:51-59  [-4 / +2 (not recommended — see risk)]
- **smell**: Two identical method bodies: `SyncHealth(int _)` and `SyncHealth()` both do `networkHealth.Value = health.GetHealth;`. They exist because HealthComponent's events have two different signatures.
- **change**: Keep the parameterless one and subscribe the int events with `health.OnDamage += _ => SyncHealth();`. Note the current code can unsubscribe correctly because it uses method groups, so if this change is made, OnDestroy must store the delegates or the unsubscribes become no-ops. Given that trap, the cleanest option is to leave the duplication alone.
- **risk**: HIGH relative to benefit: lambdas cannot be unsubscribed by reference, so a naive collapse silently breaks the OnDestroy cleanup. Listed for completeness; my recommendation is to skip it.


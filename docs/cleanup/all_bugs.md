## Assets/Game/Scripts/Vehicles/DuneFoil/Foil/FoilLift.cs:253 (call), 320-397 (ProbeGround)
- **bug**: Inverted ceiling sentinel: the slope-lookahead probe returns the LOWEST surface under the point ahead instead of the highest, so the craft's climb braking silently switches off. SampleSlope calls `ProbeGround(ahead, float.NegativeInfinity, out float aheadY, out _)` with a comment saying "Pass negative infinity to take whatever is there." But ProbeGround's filter is `if (y > ceiling) { /* remember lowest-above */ continue; }`. With ceiling = -Infinity every finite hit satisfies `y > ceiling`, so every hit is diverted into the above-ceiling bucket, the `if (y > bestY)` highest-surface branch never executes, `found` stays false, and the method falls through to `if (foundAbove) { groundY = lowestAboveY; }` — returning the lowest hit. The intended "accept anything" sentinel is +Infinity, which is exactly what the sibling call in SampleGround uses at line 272 (`float ceiling = groundPrimed ? GroundY + maxGroundStep : float.PositiveInfinity;`).
- **failure**: Sail the DuneFoil toward a dune with any second collider along the 200 m forward ray — a rock, a relay-outpost floor slab, a ruin, a bridge deck, anything with a lower surface than the dune face. ProbeGround returns that lower surface as GroundYAhead. FoilPhysics.ClimbGrade(aheadY - GroundY, 18) then comes out strongly negative, DuneFoilLocomotion.IntegrateVelocity:266-270 only subtracts when `foil.ClimbDeceleration()` is non-zero and FoilPhysics.SlopeDeceleration returns 0 for grade <= 0, so the craft climbs the dune face at full speed with zero braking and FoilLift.IsStalling never becomes true. Concretely: craft at (x, z) on sand at y=100 heading at a 25-degree face; the ray 18 m ahead crosses a rock top at y=112 and the terrain beneath it at y=104 -> aheadY = 104 is returned instead of 112, grade = (104-100)/18 = +0.22 when the truth is +0.67, so instead of 14 * (0.67/0.30)^3-scale deceleration (well past stallGrade = 0.30) the craft gets 14 * (0.22/0.30)^2 = 7.5 m/s^2 — and in the common case where the lower hit is BELOW the craft's own ground line the grade goes negative and the deceleration is exactly 0. On open dunes where the ray hits exactly one collider the bug is invisible (a single hit makes lowestAboveY == bestY), which is why it survived: it only misbehaves near props and structures.
- **fix**: Change line 253 from `float.NegativeInfinity` to `float.PositiveInfinity` so the lookahead probe genuinely accepts any surface and takes the highest. The comment at 250-252 already states the intent ("No acceptance window on the lookahead. A wall ahead SHOULD read as an unclimbable grade"). Optionally also guard the sentinel by taking a `bool useCeiling` rather than a magic float. Cover it with an EditMode test that puts two colliders under the lookahead point and asserts GroundYAhead is the upper one — FoilPhysicsTests only exercises the pure functions and DuneFoilCraftTests never touches ClimbGrade or GroundYAhead, so nothing catches this today.
- **verified**: Read ProbeGround end to end and traced both call sites. Confirmed the `y > ceiling` comparison is unconditional, that `found` can only be set inside the else path, and that SampleGround uses +Infinity for the same "no ceiling" meaning at line 272. Confirmed with grep that no test references ProbeGround, ClimbGrade (on FoilLift), GroundYAhead or IsStalling.

## Assets/Game/Scripts/Vehicles/Rover/Core/RoverController.cs:114,128,168,245 + RoverMovementController.cs:19-28,36 + WheelRotator.cs:51-57
- **bug**: Two independent `moveSpeed` fields; the one designers set on RoverController governs nothing except the wheel animation, so wheels spin at the wrong rate and the tunable is a no-op. RoverController multiplies its unit direction by its own `moveSpeed` and passes the result to `movementController.SetTargetDirection(...)`, which immediately does `targetDirection = direction.normalized` — discarding the magnitude. UpdateMovement then uses RoverMovementController's own `moveSpeed`. Meanwhile WheelRotator asks RoverController for `GetCurrentMovementVelocity()`, which returns the pre-normalisation `currentTargetDirection`, and derives the wheel spin from its magnitude — i.e. from the field that has no effect on motion.
- **failure**: Serialized values in Assets/Game/Prefabs/Vehicles/RoverNoHierarchy.prefab (the live one, referenced from Aleksander test scene.unity which is enabled in EditorBuildSettings): RoverController.moveSpeed = 3, RoverMovementController.moveSpeed = 5, WheelRotator.wheelRadius = 1.6 on all four wheels. Body translates at up to 5 m/s. Wheel circumference = 2*pi*1.6 = 10.053 m, so WheelRotator computes 3/10.053*360 = 107.4 deg/s when the correct rolling rate for 5 m/s is 5/10.053*360 = 179.0 deg/s — the wheels visibly under-rotate by 40% and the rover skates. Separately, setting RoverController.moveSpeed to 30 in the Inspector changes nothing about how fast the rover moves; it only makes the wheels spin 10x too fast. Rover.prefab has the same split with RoverMovementController.moveSpeed = 10 against RoverController's code default of 3, a 70% error.
- **fix**: Delete `moveSpeed` from RoverController and delete `GetCurrentMovementVelocity()`; have WheelRotator read the authoritative speed from `RoverMovementController.GetCurrentSpeed()` (which already exists at RoverMovementController.cs:57 and currently has zero callers) and take the sign/direction from the movement controller too. That removes one field, one method and one of the two speed sources — net negative. If the Rover subsystem is being deleted per the removal list, this is moot.
- **verified**: Traced the value through all three files and confirmed `SetTargetDirection` normalizes unconditionally at RoverMovementController.cs:23. Read the actual serialized field values out of both prefab YAMLs (RoverController guid 5cdaf4928be0f9042b25d9716da44896 -> moveSpeed 3; RoverMovementController guid 34e271b65d1b2be409f32815e1f521a2 -> moveSpeed 5 in RoverNoHierarchy, 10 in Rover; WheelRotator guid 02e2e3e49de24634d9799adba66e8fc6 -> wheelRadius 1.6 x4). Confirmed GetCurrentSpeed() has no callers via a member-level scan of every .cs in Assets/.

## Assets/Game/Scripts/Vehicles/Rover/Core/RoverMovementController.cs:36-41 driven from RoverController.cs:110-118,133-139,146-153
- **bug**: The obstacle-avoidance "backup" state cannot back up — the rover drives forward into whatever it just detected. RoverController's backup branch sets the target direction to `-currentMoveDirection * moveSpeed` and holds it for `backupTime`. But RoverMovementController translates with `movement = roverTransform.forward * currentSpeed * Time.deltaTime` where `currentSpeed = Mathf.Lerp(currentSpeed, targetSpeed, ...)` and `targetSpeed` is either `moveSpeed` or 0 — never negative. There is no code path in the file that produces reverse translation. The reversed target direction only feeds the `Quaternion.RotateTowards` at line 47, which turns the hull at `rotationSpeed` deg/s while it keeps driving forward.
- **failure**: With the shipped RoverNoHierarchy.prefab values (rotationSpeed = 45 deg/s, backupTime = 2 s default, moveSpeed = 5): the forward raycast from ThingMount hits a wall at 7 m, `canMoveForward` goes false, RoverController sets `backupUntilTime = Time.time + 2` and commands the reverse direction. Over the 2 s window the rover can yaw at most 45*2 = 90 degrees, so for the entire "backup" it is still pointed within 90 degrees of the wall and translating forward at up to 5 m/s — it presses into the obstacle for 2 seconds, then the commit timer expires and the cycle repeats. The rover never reverses out of a dead end; it grinds against it.
- **fix**: In RoverMovementController.UpdateMovement, drive along the commanded direction rather than always along `transform.forward`: `roverTransform.position += targetDirection * currentSpeed * Time.deltaTime` (and keep the RotateTowards for facing), or add an explicit reverse flag that allows negative currentSpeed. Either is a 1-2 line change. Note this is the same root cause as the previous bug — the movement controller throws away the commanded vector and keeps only its direction for rotation — so fixing that discards this one too. Again moot if the Rover subsystem is removed.
- **verified**: Read both files fully. Confirmed `targetSpeed` at RoverMovementController.cs:36 is `moveSpeed` or `0f` with no negative branch, that `currentSpeed` is only ever Lerped toward it, and that line 40 unconditionally uses `roverTransform.forward`. Confirmed rotationSpeed = 45 and moveSpeed = 5 from the serialized RoverMovementController block in RoverNoHierarchy.prefab.

## Assets/Game/Scripts/Creatures/Humanoid/HumanoidSpineMotion.cs:181
- **bug**: `float lean = leanIntoRun * d.Duty * smoothedEffort;` multiplies the forward chest lean by the gait's DUTY, which is not a speed signal. Duty is the fraction of the cycle a foot is airborne — 0.42 at a walk, 0.60 at a run on this rig — so it silently scales the authored lean to ~0.5x and adds a second, undocumented 1.43x ramp on top of the smoothedEffort envelope that is already doing that job. The likely intent is `leanIntoRun * smoothedEffort`; Diagnostics exposes Duty, Phase and AchievedSpeed but not RunBlend, so Duty appears to have been reached for as a stand-in.
- **failure**: With the shipped defaults (HumanoidSpineMotion.leanIntoRun = 5, HumanoidLocomotion.walkSwingDuty = 0.42, runSwingDuty = 0.60): at full speed smoothedEffort converges to 1 and d.Duty = 0.60, so the chest leans 5 * 0.60 * 1 = 3.0 degrees. The field's own tooltip (line 53-55) says 'Degrees the chest leans forward at top speed', i.e. 5.0 degrees. Set leanIntoRun to its Range maximum of 20 and the machine leans 12, not 20. Standing still the term is harmless because smoothedEffort is 0.
- **fix**: Drop the `* d.Duty` factor: `float lean = leanIntoRun * smoothedEffort;`. If a run-vs-walk shape is genuinely wanted there, use smoothedEffort squared or expose RunBlend on Diagnostics rather than borrowing Duty.
- **verified**: Read the arithmetic at HumanoidSpineMotion.cs:167-184 against the serialized defaults in HumanoidSpineMotion.cs:56 and HumanoidLocomotion.cs:56,60, and against AlternatingGait.Duty (GaitPatterns.cs:103) which returns Lerp(walkDuty, runDuty, runBlend). Confirmed d.Duty is set from CurrentDuty = gaitPattern.Duty(RunBlend) at LeggedLocomotion.cs:376. No test asserts the lean magnitude (HumanoidArmSwingTests only asserts ChestYaw), so nothing pins the current value.

## Assets/Game/Editor/Creatures/HorseBuilder.cs:41, Assets/Game/Editor/Creatures/CrabWalkerBuilder.cs:30, Assets/Game/Editor/Tests/HorseRigWiringTests.cs:26
- **bug**: Three hard-coded asset paths spell the directory `Assets/Game/Prefabs/agents/creatures` (lowercase) while the actual directory is `Assets/Game/Prefabs/Agents/Creatures`. Unity's AssetDatabase resolves paths through the OS filesystem, so this is invisible on macOS and Windows (case-insensitive) and breaks on a case-sensitive filesystem — a Linux build agent or headless test runner.
- **failure**: On Linux CI: HorseRigWiringTests.ThePrefabCarriesTheMountAndAgentRig calls AssetDatabase.LoadAssetAtPath<GameObject>("Assets/Game/Prefabs/agents/creatures/HorseRobot.prefab"), which returns null because no such path exists, and the test fails at its first assertion ('Horse prefab missing at ... -- run Tools/Creatures/Build Horse Robot Prefab') even though the prefab is present. Tools/Creatures/Build Horse Robot Prefab and the crab builder likewise fail to save, because PrefabUtility.SaveAsPrefabAsset cannot create an asset under a directory the AssetDatabase does not have.
- **fix**: Correct the three constants to `Assets/Game/Prefabs/Agents/Creatures`. Better: derive them from one shared const, since all three currently repeat the same string.
- **verified**: `ls -d Assets/Game/Prefabs/*/` and `ls -d Assets/Game/Prefabs/Agents/*/` confirm the on-disk spelling is Agents/Creatures; grep -rn 'Assets/Game/Prefabs' over Assets/Game/Editor/Creatures/*.cs and HorseRigWiringTests.cs confirms all three literals are lowercase. Latent rather than currently-failing on this developer's macOS machine — reported because it is a positively verified path mismatch with a concrete platform where it bites, and because the memory note about build scripts and Assets/ paths says this class of bug has been hit before. Editor/ is another auditor's scope; flagged here because it is the only way the Crab and Horse rigs in my scope can be rebuilt.

## Assets/Game/Scripts/World/Streaming/Core/WorldStreamer.cs:369 (inside PreloadChunksAroundPosition)
- **bug**: The single-position preload overload resolves its chunk with `config.WorldToChunkCoord(worldPos)`, which CLAMPS out-of-grid positions to the nearest edge chunk. Its sibling `PreloadChunksAroundPositions` (line 408) deliberately does not: it calls `config.TryGetStreamingCoord(...)` and `continue`s on off-world positions, with the comment 'Positions that belong to a deliberately off-grid scene (the minigame arena) must not be clamped into a real edge chunk here — WorldToChunkCoord clamps, so the off-world test has to happen before calling it.' The singular overload never got that guard, so it does exactly what the comment forbids. WorldStreamingConfig.cs:52 and ChunkGrid.cs:52-54 both document the same trap ('WorldToChunkCoord clamps out-of-bounds positions to the nearest edge chunk rather than signaling invalidity, so callers that need to distinguish "outside the world" ... must check this first'). The clamped coord is then fed straight to GetChunksInRadius + EnqueueLoad, and MarkInitialChunksLoaded() is fired for a set of chunks nobody is standing in.
- **failure**: Offline/single-player. A player object exists at a position beyond WorldStreamingConfig.offWorldDistance (2000 m) from the 8x6 grid — the codebase's own reference case, documented in WorldStreamingConfig.cs:32, is the minigame arena at ~16.5 km east. PlayerController.Start -> WaitForStreamerThenPreload -> `streamer.PreloadChunksAroundPosition(transform.position)` (PlayerController.cs:58). ToCoord clamps x=~16500 to column 7, so the streamer additively loads the 3x3 block around Chunk_7_y — including Chunk_7_5, a 41.5 MB settlement scene, and Chunk_7_1 at 17.4 MB. Correct behaviour (what the plural overload gives) is to load nothing and leave the arena alone. Same path via InteriorManager.cs:263 for an interior whose stored return position is off-grid. Note the trigger condition is 'caller position past offWorldDistance', which I verified from the code; I did not execute the arena scene to confirm a player is positioned there before Start runs, so treat the arena as the worked example rather than a reproduced repro.
- **fix**: Collapse the overload into the guarded one — `public void PreloadChunksAroundPosition(Vector3 worldPos, Action onComplete = null) => PreloadChunksAroundPositions(new[] { worldPos }, onComplete);`. This is the refactor listed above: it deletes 34 lines, adds 2, and removes the second copy of the algorithm so the guard cannot drift out of sync again.
- **verified**: Read both overloads side by side (lines 360-433) plus ChunkGrid.ToCoord (clamps, lines 56-68), ChunkGrid.TryGetStreamingCoord (returns false past offWorldDistance, lines 103-109) and WorldStreamingConfig.offWorldDistance = 2000 in the .asset. Enumerated all call sites with a repo-wide grep: PlayerController.cs:58 and InteriorManager.cs:263 use the unguarded singular; only NetworkGameManager.cs:259 uses the guarded plural.

## Assets/Game/Scripts/World/Streaming/Core/WorldStreamer.cs:950-951 (HandleSceneEvent, unload branch)
- **bug**: Missing scene-identity check on the unload completion path, reported as LOW confidence because I could not construct a repro I am sure Netcode permits. The load branch correctly matches the event to the pending operation (`sceneEvent.SceneEventType == LoadEventCompleted && pendingSceneName != null && sceneEvent.SceneName == pendingSceneName`). The unload branch matches on `SceneEventType == UnloadEventCompleted && pendingSceneName == null` only — and `pendingSceneName == null` is precisely the state ExecuteUnload sets (line 874), so ANY UnloadEventCompleted arriving while a chunk unload is in flight is attributed to that chunk. InteriorManager.cs:473 unloads interior scenes through the same NetworkManager.Singleton.SceneManager, so a second unload source exists.
- **failure**: Would be: chunk (3,2) unload in flight (pendingCoord=(3,2), pendingSceneName=null, operationInProgress=true); an unrelated UnloadEventCompleted arrives for an interior scene; WorldStreamer marks (3,2) NotLoaded and drops it from loadedScenes while the chunk scene is still resident, so UpdateChunkLoading can then issue a duplicate additive LoadScene for it. HOWEVER — Netcode's global SceneEventInProgress flag (which is exactly why RetryOperation exists at lines 831-842) appears to serialise the two sources, so I could NOT demonstrate an overlap window. Reporting per the brief's honesty bar as a hardening item, not a confirmed bug; do not 'fix' it as a bug without reproducing it.
- **fix**: If it is ever reproduced: store the unloading scene's name in ExecuteUnload (a separate field, since pendingSceneName==null is load-vs-unload discriminator) and add `&& sceneEvent.SceneName == pendingUnloadSceneName` to the branch. ~3 lines.
- **verified**: Read HandleSceneEvent, ExecuteUnload, RetryOperation and FinishOperation in full; grepped every SceneManager load/unload call site in Assets/Game/Scripts to find the other Netcode scene-event sources (InteriorManager.cs:449/473, MainMenuUI.cs:63/96/100, SaveHotkeys.cs:73, StartingGameManager.cs:35). Could not rule out that Netcode's busy flag makes the overlap impossible, hence low confidence.

## Assets/Game/Scripts/Agents/Perception/PerceptionModule.cs:147-157 (HasLineOfSightFrom)
- **bug**: Line-of-sight is decided from an unsorted Physics.RaycastAll result. The code does `RaycastHit[] hits = Physics.RaycastAll(origin, dir, distance, occlusionLayers)` and then, on the FIRST array element that is not self, returns true if it is the target and false otherwise. Unity documents RaycastAll as returning hits in no particular order — it does not sort by distance — so the loop's verdict depends on collider iteration order inside the physics engine rather than on what is actually nearest. The author clearly assumed nearest-first: `return false` on the first non-self hit only makes sense for a sorted array.
- **failure**: State verified from the shipped assets: PatrolRobot.prefab (and PatrolRobot 1/2/3, DeathmatchBot) has PerceptionModule.occlusionLayers = m_Bits 641 = layers {0, 7, 9}, and AIM_GruntPoor.asset — the aim profile assigned to all five of those prefabs — has requireLineOfSight: 1. PlayerCharacter.prefab's root is m_Layer: 0 (Default), which is inside that mask. So with the player standing behind a Default-layer wall, inside FIRE_PistolBurst.maxRange, the RaycastAll from the muzzle returns BOTH the wall collider and the player's collider. If the physics engine happens to place the player's collider first in the array, the loop hits `t == target` before it ever sees the wall and returns true: AgentRangedCombatModule.cs:176 keeps `engaged` true, holds station and fires a full burst through solid geometry. The same call path also gates acquisition (AgentTargeting.cs:352 -> IsVisible -> HasLineOfSightFrom), so agents can also acquire through walls. Order is not guaranteed and not stable, so the symptom is intermittent — which is why it reads as 'the robots sometimes shoot through walls' rather than a reproducible break.
- **fix**: Do not trust array order. Scan all hits, skipping self and self's children, and keep the one with the smallest RaycastHit.distance; then return true only if that nearest non-self hit is the target or a child of the target. Roughly: track `float nearest = float.MaxValue; Transform nearestT = null;` in the existing loop, then a single comparison after it. Same file, same method, no new type, roughly +4 lines net.
- **verified**: Read the method; confirmed the early-return-on-first-element logic. Confirmed the mask actually contains the target's layer by decoding m_Bits 641 from PatrolRobot.prefab/DeathmatchBot.prefab (bits 0,7,9) and reading m_Layer: 0 on PlayerCharacter.prefab. Confirmed requireLineOfSight is on by reading Assets/Game/ScriptableObjects/Weapons/AIM_GruntPoor.asset:18 and confirming its GUID appears in all five robot prefabs. Confirmed this is the only LoS implementation the combat path uses (AgentRangedCombatModule.cs:395 routes to it; AgentTargeting.cs:352 routes to it via IsVisible).

## Assets/Game/Editor/Agents/EntityProfileEditors.cs:107 (EntityProfileEditorUtils.SetupBaseComponents)
- **bug**: The prefab generator looks up a serialized property by the wrong name and the miss is swallowed. It calls `SetObject(controller, "motorComponent", motor)`, but AgentController's field is declared `[SerializeField] private MonoBehaviour MotorComponent;` (AgentController.cs:14) — capital M. SerializedObject.FindProperty is case-sensitive, so it returns null, and SetObject's guard `if (p != null) { ... }` (lines 55-60) discards the assignment with no log and no exception.
- **failure**: Add EntityProfile_BaseAgent (or _GenericEnemy, or _Vehicle — all three route through SetupBaseComponents) to any prefab and click the green "Generate" button. Every other field is wired correctly: `animatorDriver` on the next line has matching casing and gets set, as do the NavMeshAgentMotor's `agent`, HealthComponent's `maxHealth`/`currentHealth` and HealthReactionModule's `despawnDelay`. But AgentController's Motor Component slot stays "None", and the console prints the cheerful `[EntityProfile] Base Agent generated on <name>` regardless. The generated prefab's YAML then has no `MotorComponent:` value while every hand-authored prefab does (confirmed: PatrolRobot.prefab:317 `MotorComponent: {fileID: 836584490720039465}`). At runtime AgentController.ResolveMotor() auto-resolves the first IMovementMotor in children, so the agent still walks — which is exactly why the defect has survived: the tool silently half-works and the symptom is invisible until someone adds a second IMovementMotor to the hierarchy, at which point the auto-resolve picks whichever GetComponentsInChildren returns first instead of the one the profile intended.
- **fix**: Change the string on line 107 from "motorComponent" to "MotorComponent". One-character fix, no other change. Do NOT instead rename the C# field to lowercase — that would orphan the `MotorComponent:` key in every one of the 33 prefabs and scenes that reference AgentController, silently nulling their motor assignment. If the four EntityProfile_* scripts are deleted per my removals list, this whole file goes and the bug goes with it.
- **verified**: Read AgentController.cs:14 for the declared field name; read EntityProfileEditors.cs:55-60 for SetObject's silent null guard and :107 for the call; confirmed the serialized key casing in the shipped prefab with grep on Assets/Game/Prefabs/Agents/Robots/PatrolRobot.prefab (`MotorComponent: {fileID: ...}`). Cross-checked the adjacent working calls ("animatorDriver", "agent", "maxHealth", "despawnDelay") against their declarations to confirm this is the only casing mismatch in the file.

## Assets/Game/Editor/Creatures/HorseBuilder.cs:41, Assets/Game/Editor/Tests/HorseRigWiringTests.cs:26, Assets/Game/Tests/EditMode/HorseLocomotionTests.cs:18, Assets/Game/Tests/EditMode/HumanoidLocomotionTests.cs:30, Assets/Game/Tests/EditMode/HumanoidArmSwingTests.cs:24
- **bug**: Five hardcoded asset paths use the wrong case for two directory components. All five spell the path "Assets/Game/Prefabs/agents/creatures/HorseRobot.prefab" (or .../HumanoidRobot.prefab), but the directories on disk are `Prefabs/Agents/Creatures/`. This is latent on the developers' machines because the default macOS APFS volume is case-insensitive; it is a hard failure anywhere the filesystem is case-sensitive.
- **failure**: Run the EditMode suite (or HorseBuilder) on a case-sensitive filesystem — Linux CI, a Docker build agent, or a case-sensitive APFS volume. AssetDatabase.LoadAssetAtPath returns null for the miscased path, so HorseLocomotionTests, HumanoidLocomotionTests, HumanoidArmSwingTests and HorseRigWiringTests all fail at setup with a null prefab, and HorseBuilder — which writes to the same constant — creates a SECOND prefab tree at the lowercase path instead of updating Assets/Game/Prefabs/Agents/Creatures/HorseRobot.prefab, leaving two divergent copies of the horse rig in the project.
- **fix**: Correct the case in all five string constants to "Assets/Game/Prefabs/Agents/Creatures/...". Five one-word edits, no logic change. Worth doing now rather than at the moment CI is introduced, because HorseBuilder's failure mode is a duplicated asset rather than an error.
- **verified**: Read the five literals; compared against the real directory names from `find Assets/Game/Scripts/Agents -type d` and the prefab listing, which show `Assets/Game/Prefabs/Agents/Creatures/`. Also the reason I did NOT list HorseRobot/HumanoidRobot/CrabWalker4/6/8 as removals despite their 0 GUID refs — these string paths are how they stay alive, and the pre-computed GUID scan cannot see them.

## Assets/Game/Scripts/Items/Core/InventoryItem.cs:21 (crash site: Assets/Game/Scripts/Core/Registry/Registry.cs:11)
- **bug**: `public string ID { get; set; }` is an auto-property, which Unity never serializes, and the only writer is an `#if UNITY_EDITOR` OnValidate. So the ID exists only in the editor's in-memory copy: in a player build every InventoryItem's ID is null.
- **failure**: Make a player build and enter a scene containing RegistryLoader. `LoadItems()` calls `Registry<InventoryItem>.Register(item)`, which executes `entries[value.ID] = value` with value.ID == null — Dictionary<string,T>'s indexer throws ArgumentNullException on a null key. RegistryLoader.Awake dies on the first item, so SaveablePrefabRegistry.LoadAll() never runs either and the entire item/save system is unregistered. (Verified: no `ID:` key exists in any of the 20 InventoryItem .asset files; `grep -rn '\.ID\s*=' Assets --include=*.cs` finds only InventoryItem.cs:40 and two test files; no `[field: SerializeField]` anywhere in Assets.) In-editor play mode hides this because OnValidate has already run on the loaded assets.
- **fix**: Back the property with a serialized field: `[SerializeField] private string id; public string ID { get => id; set => id = value; }` (or `[field: SerializeField] public string ID { get; set; }`), then re-save the item assets so the GUID is written into each .asset. Defensively, make Registry.Register skip null/empty IDs.
- **verified**: Read InventoryItem.cs, Registry.cs, RegistryLoader.cs; dumped all 20 item .asset files and confirmed no serialized ID field; grepped for every other ID writer.

## Assets/Game/Prefabs/Items/Equipment/ExpeditionBackpack.prefab:4590-4601 + Assets/Game/ScriptableObjects/Items/*.asset
- **bug**: All 10 of the live player pack's starting items reference InventoryItem assets in Assets/Game/ScriptableObjects/Items/, which is not a Resources folder, so RegistryLoader (`Resources.LoadAll<InventoryItem>("Items")`) never registers them and nothing can resolve them by ID.
- **failure**: In the editor: play PlayerCharacter (which carries BackpackSaveable, RequireComponent BackpackController), save, then load. BackpackSaveCodec.Capture writes each item's ID; RestoreCompartment then does `Registry<InventoryItem>.Get(id)`, gets null for all six strap items and all four main items, logs "[Save] Backpack item '<guid>' is not in the registry — its slot was left empty. Was the item asset deleted?" ten times and calls RestoreSlot(i, null). The player's whole starting kit (GrapplingHook, RuinScanner, AntiGravityFlask, Lasso, Leash, RocketArtifact ×2 compartments) is gone after every load. The same null resolve hits PlayerInventoryNetwork.HandleNetworkListChanged/TryAddItemServerRpc and EquipmentNetworkSync.EquipClientRpc, so taking one of these items to the hotbar on PlayerCharacterNetworked yields an empty slot.
- **fix**: Repoint ExpeditionBackpack's startingStrapItems/startingMainItems at the identical Resources/Items/Artifacts assets (GrapplingHook, RuinScanner, AntiGravityPotion, Lasso, Leash, RocketTurret) and delete the six ScriptableObjects/Items duplicates. Copy the correct Leash icon (guid e8e082f4…) onto Resources/Items/Artifacts/Leash.asset, which currently shows the Lasso icon.
- **verified**: Grepped the six asset GUIDs across Assets+ProjectSettings+Packages (only the two backpack prefabs match); compared each pair's itemPrefab/icon GUIDs; read RegistryLoader.LoadItems, Registry.Get, BackpackSaveCodec.RestoreCompartment, PlayerInventoryNetwork and EquipmentNetworkSync; confirmed BackpackSaveable is on PlayerCharacter.prefab via refs.tsv.

## Assets/Game/Scripts/Weapons/Projectiles/BasicProjectile.cs:16,30-36,47-69
- **bug**: `lastPosition` is never seeded, so the first collision check raycasts from the world origin instead of from the projectile's previous position. UpdateMovement moves, then tests `Time.time - lastCollisionCheck >= checkInterval` (lastCollisionCheck starts at 0 and Time.time is seconds-since-load, so it is true on the very first frame), then CheckCollision uses lastPosition == Vector3.zero.
- **failure**: Equip the BasicGun item (Resources/Items/Artifacts/basicgun.asset → Gun.prefab; grantable from DevInventoryUI, or the gunPickup in 'Aleksander test scene') anywhere away from (0,0,0) — say the player at (1500,30,900) — and fire. On the projectile's first Update, CheckCollision runs `Physics.Raycast((0,0,0), fireDir, out hit, ~1758 + 0.001, hitMask=all layers)`. Any collider standing between the world origin and 1.7 km along the fire direction registers as a hit: HandleHit applies 10 damage to that object's HealthComponent and, with destroyOnHit = 1 on projectile.prefab, destroys the projectile roughly one frame after it spawns. The bogus hit is also announced by the OnImpact Debug.Log naming an object nowhere near the player.
- **fix**: Seed the position: set `lastPosition = transform.position` at the end of Projectile.Initialize (or in an overridden Initialize here), and raycast along `(transform.position - lastPosition)` rather than `direction`. The checkInterval throttle can go at the same time — at 0.01 s it fires every frame anyway.
- **verified**: Read BasicProjectile.cs and Projectile.cs (no writer to lastPosition before the first CheckCollision); confirmed projectile.prefab has hitMask m_Bits 4294967295 and destroyOnHit 1; confirmed BasicGun.Fire's Instantiate→Initialize→StartLifetime order.

## Assets/Game/Scripts/Items/Artifacts/Leash/Leash.cs:70,81-84,279-301
- **bug**: Leash GameObjects are DontDestroyOnLoad'ed and then never disposed when an endpoint dies. FixedUpdate/LateUpdate simply `return` when aTransform or bTransform is null ("we DO NOT auto-dispose … so we can inspect it in the Hierarchy"), Snap() can only trigger while both ends are alive, LeashAttachable.OnDestroy explicitly does not dispose, and LeashArtifact.OnDestroy clears its list without disposing.
- **failure**: Equip the Leash, left-click an NPC to create a held leash, then left-click a second already-leashed object to terminate it (LeashArtifact.Use removes it from _heldLeashes, so the artifact no longer knows about it). Now let either endpoint be destroyed — the NPC dies, or its object is unloaded with a streaming chunk. The Leash GameObject survives (DontDestroyOnLoad exempts it from scene unload), keeps its LineRenderer showing the last computed rope in mid-air, and is unreachable by any code path that could destroy it. Every leash used in a session accumulates for the rest of the process, across every scene load.
- **fix**: In FixedUpdate, when an endpoint transform has become null, call Dispose() instead of returning (the diagnostic hold-open was for chasing the endpoint-destruction bug and should not ship). Also have LeashAttachable.OnDestroy dispose the leashes it lists, and drop the DontDestroyOnLoad or re-parent leashes to a persistent scene root that is cleaned up with the session.
- **verified**: Read Leash.cs, LeashAttachable.cs and LeashArtifact.cs end to end and traced every caller of Dispose()/Snap(): LeashArtifact.Update (held leashes only) and Leash.Snap (requires both transforms non-null to be reached).

## Assets/Game/Scripts/Weapons/Core/Weapon.cs:120
- **bug**: `if (Time.time >= nextFireTime && !IsReadyToFire)` is a contradiction: `IsReadyToFire => Time.time >= nextFireTime` (line 54), so the condition is `X && !X` and is always false. OnFireRateReady can never be raised.
- **failure**: Any weapon, any frame: the body `OnFireRateReady?.Invoke()` is unreachable, so a listener that wanted to know when the cooldown expired (e.g. a crosshair-ready indicator) would never be called. No visible symptom today only because the event has zero subscribers — which makes the whole block dead weight either way.
- **fix**: Delete the block together with the OnFireRateReady event (see the Weapon/Magazine dead-API refactor). If the notification is actually wanted, track a `wasReady` bool and fire on the false→true edge.
- **verified**: Read Weapon.cs lines 40-125 and 54; grepped for OnFireRateReady subscribers across Assets (none).

## Assets/Game/Scripts/Items/Artifacts/Gadgets/AntiGravityPotion.cs:29-32
- **bug**: `if (useSound.Guid == null)` compares a non-nullable struct to null. FMOD.GUID (Assets/Plugins/FMOD/src/RuntimeUtils.cs:11) is a struct with operator==(GUID,GUID), so the comparison is lifted to GUID? and is always false — the guard never triggers. The correct test, used correctly by RuinScannerArtifact:188 (`!discoverySound.IsNull`), is EventReference.IsNull.
- **failure**: Leave the AntiGravityPotion prefab's useSound unassigned and use the potion. The intended warning "Use sound GUID not assigned for AntiGravityPotion!" is never printed, so the missing FMOD event stays silent-but-unreported; instead UsableItem.TryUse (line 26) has already handed the empty EventReference to AudioManager.Instance.PlayEvent. The `return` is also a no-op — nothing follows it in Use().
- **fix**: Delete the whole block (RegisterEffect has already run and nothing follows the return), or, if the diagnostic is wanted, move it above RegisterEffect and test `useSound.IsNull`.
- **verified**: Read AntiGravityPotion.cs, UsableItem.cs, Plugins/FMOD/src/EventReference.cs (public FMOD.GUID Guid) and Plugins/FMOD/src/RuntimeUtils.cs (struct GUID with IsNull and operator==).

## Assets/Game/Scripts/Items/Artifacts/Gadgets/LightningSpell.cs:18-23
- **bug**: `spawnPoint = aimProvider.GetRayCast(raycastDistance)?.point + Vector3.up * spawnHeightOffset ?? Vector3.zero;` collapses a no-hit aim to Vector3.zero, and the VFX is then spawned unconditionally at that point.
- **failure**: Equip LightningSpell and use it while aiming at open sky (no collider within raycastDistance = 500 m). AimProvider.GetRayCast returns null, the lifted addition yields null, `?? Vector3.zero` makes spawnPoint (0,0,0), and Instantiate drops the Lightning VFX prefab at the world origin — visible as a lightning strike at the corner of the map instead of nothing happening.
- **fix**: `var hit = aimProvider.GetRayCast(raycastDistance); if (hit == null) return; spawnPoint = hit.Value.point + Vector3.up * spawnHeightOffset;`
- **verified**: Read LightningSpell.cs and AimProvider.GetRayCast (returns null and logs a warning on a miss); confirmed LightningSpell.prefab wires lightningVFXPrefab (Prefabs/VisualEffects/Lightning/Lightning.prefab) so the Instantiate branch is the live one.

## Assets/Game/Editor/Vehicles/OrnithopterBuilder.cs:36-37 and :106-107; Assets/Game/Editor/Vehicles/WingPackBuilder.cs:25-27 and :70-71; Assets/Game/Editor/Creatures/HorseBuilder.cs:41 and :110-111; Assets/Game/Editor/Creatures/CrabWalkerBuilder.cs:30 and :108-109
- **bug**: Four prefab builders still write to their pre-restructure output paths, so re-running them creates a duplicate orphan prefab instead of updating the live one. This is the exact failure mode that DuneFoilBuilder.cs:33-37 and DesertCrawlerBuilder.cs:40-41 document as already found and fixed for themselves — the migration was only half done. OrnithopterBuilder.PrefabPath = "Assets/Game/Prefabs/agents/vehicle/DuneOrnithopter.prefab"; the live prefab is at Assets/Game/Prefabs/Agents/Vehicles/Aircraft/DuneOrnithopter.prefab (there is no `vehicle` folder under Agents at all — the dirs are Creatures, Robots, Vehicles, Weapons). WingPackBuilder has the same stale CraftPath plus PrefabPath = "Assets/Game/Prefabs/items/WingPack.prefab" vs the live Assets/Game/Prefabs/Items/Equipment/WingPack.prefab. HorseBuilder and CrabWalkerBuilder write to "Assets/Game/Prefabs/agents/creatures/" while disk is Assets/Game/Prefabs/Agents/Creatures/.
- **failure**: Open Unity, run Tools ▸ Vehicles ▸ Build Dune Ornithopter Prefab. Line 106 Directory.CreateDirectory creates Assets/Game/Prefabs/agents/vehicle/, line 107 SaveAsPrefabAsset writes a brand-new prefab there with a fresh GUID, line 111 logs '[Ornithopter] Built Assets/Game/Prefabs/agents/vehicle/DuneOrnithopter.prefab: ... 30-bone rig, prone seat.' — success. The DuneOrnithopter instance in persistentScene still points at the Aircraft/ GUID and is completely unchanged, so every wiring fix the author just made is invisible in game while the build log says it landed. Identical for WingPack (writes Prefabs/Items/WingPack.prefab beside the real Prefabs/Items/Equipment/WingPack.prefab), HorseRobot and CrabWalker4/6/8.
- **fix**: Change the four constants to the on-disk paths: Assets/Game/Prefabs/Agents/Vehicles/Aircraft/DuneOrnithopter.prefab, Assets/Game/Prefabs/Items/Equipment/WingPack.prefab, Assets/Game/Prefabs/Agents/Creatures/HorseRobot.prefab, and PrefabDir = Assets/Game/Prefabs/Agents/Creatures. Then, to stop it recurring, make each builder fail loudly instead of silently: assert AssetDatabase.LoadAssetAtPath<GameObject>(PrefabPath) != null before writing, so a moved prefab errors rather than forking. (~8 lines changed, ~5 added.)
- **verified**: python script walked every Assets/... string literal in Assets/Game/Editor and Assets/Game/Tests and tested each with a case-EXACT path walk (os.listdir membership per segment, because macOS os.path.exists is case-blind). Then confirmed the real locations: os.path.exists on Assets/Game/Prefabs/Agents/Vehicles/Aircraft/DuneOrnithopter.prefab, .../Items/Equipment/WingPack.prefab, .../Agents/Creatures/{HorseRobot,Ostrich,HumanoidRobot}.prefab all True, and listed Assets/Game/Prefabs/Agents/ = [Creatures, Robots, Vehicles, Weapons] — no `vehicle`. Read DesertCrawlerBuilder.cs:40-43 and DuneFoilBuilder.cs:33-40, which describe this bug in their own comments as fixed.

## Assets/Game/Editor/Tests/OrnithopterRigWiringTests.cs:24-25 (used at :48, :157) and Assets/Game/Editor/Tests/OrnithopterWingAnimatorTests.cs:18 (used at :41)
- **bug**: Twelve tests fail unconditionally because their prefab paths were not updated by the restructure. Both fixtures load the ornithopter from "Assets/Game/Prefabs/agents/vehicle/DuneOrnithopter.prefab", a path with no counterpart on disk on any filesystem (the `vehicle` directory does not exist under Agents/), so AssetDatabase.LoadAssetAtPath returns null. OrnithopterRigWiringTests.LoadCraftPrefab (line 49) then trips Assert.IsNotNull, and it is called by 8 of the 10 tests; TheWingPackPointsAtTheCraft additionally fails on the equally stale "Assets/Game/Prefabs/items/WingPack.prefab". Only TheInventoryItemResolvesToTheHeldPack passes, because Assets/Game/Resources/Items/Artifacts/WingPack.asset is the one path that was never moved.
- **failure**: Run the EditMode suite (Tools ▸ Tests ▸ Run EditMode Tests (headless), or the Test Runner). All 10 OrnithopterRigWiringTests except TheInventoryItemResolvesToTheHeldPack, plus all 3 OrnithopterWingAnimatorTests, fail with 'Ornithopter prefab missing at Assets/Game/Prefabs/agents/vehicle/DuneOrnithopter.prefab — run Tools ▸ Vehicles ▸ Build Dune Ornithopter Prefab.' Following that instruction makes it worse, because the builder in the previous finding writes a fresh, unwired prefab to exactly that path — after which the tests pass against a prefab the game never loads.
- **fix**: Point both CraftPath constants at Assets/Game/Prefabs/Agents/Vehicles/Aircraft/DuneOrnithopter.prefab and PackPath at Assets/Game/Prefabs/Items/Equipment/WingPack.prefab. 3 lines. Fix these together with the builder paths above, or the tests will start passing against the orphan prefab.
- **verified**: Read both test files end to end. Traced OrnithopterRigWiringTests.LoadCraftPrefab (lines 46-52) to its 8 callers (ThePrefabCarriesTheFlightAndMountRig, EveryBoneTheAnimatorNeedsResolves, TheTwoWingsCarryOppositeSigns, TheWebbedPanelsAreSkinned, TheSeatLaysTheRiderProne, TheCameraFollowsTheMountsPitch, TheCameraBoomClearsTheWingspan, SteeringIsWiredForFlight) and OrnithopterWingAnimatorTests:41. Confirmed via case-exact path walk that the loaded paths do not resolve and that the Aircraft/ and Items/Equipment/ paths do. Library/ScriptAssemblies/Assembly-CSharp-Editor.dll is dated Aug 14 00:59, so these fixtures do compile — they compile and fail, they are not excluded.

## .gitattributes:20-23 vs the on-disk location Assets/Game/Terrain/ChunkData/
- **bug**: Binary Unity terrain data is being processed by git as LF-normalized text. .gitattributes forces `*.asset text eol=lf` (line 12) and then carves out the terrain exceptions by path: `Assets/Terrain/ChunkData/*.asset binary`, `Assets/TerrainData_*.asset binary`, `Assets/*Terrain.asset binary`. All three paths are pre-restructure and match nothing — the real location is Assets/Game/Terrain/ChunkData/. So the generic text rule wins on 24 binary terrain heightmap assets, and git is free to rewrite CRLF→LF inside them on add and LF→CRLF on checkout with autocrlf set.
- **failure**: `git check-attr text eol binary -- Assets/Game/Terrain/ChunkData/TerrainData_4_3.asset` reports `text: set`, `eol: lf`, `binary: unspecified`, while `file` on the same path reports `data` and its first bytes are `0000 0000 0000 0000 0000 0016 ...` — raw binary. Any 0x0D 0x0A byte pair inside a heightmap is a candidate for silent rewriting. Concretely: a teammate on Windows (or anyone with core.autocrlf=true) clones, git expands LF to CRLF inside the heightmap payload, and Unity loads corrupted terrain — malformed heights or a failed asset load, with a clean `git status` because the file matches what git thinks it should be.
- **fix**: Retarget the three exception globs at the real path — `Assets/Game/Terrain/ChunkData/*.asset binary` — or, more robustly, replace all three with `*.asset -text` plus explicit `text eol=lf` only for the .asset files you actually want normalized. Then run `git add --renormalize .` and diff to see whether any terrain asset has already been altered in history. 3 lines changed.
- **verified**: `ls -d Assets/Terrain/ChunkData` → No such file or directory; the two other globs expand to no matches. `find Assets -name '*.asset' -path '*errain*'` → 24 files, all under Assets/Game/Terrain/ChunkData/. `git check-attr text eol binary` on one of them returned text: set / eol: lf. `file` and `xxd` on the same file confirmed it is binary, not YAML.

## Repo-wide: git index vs working tree, 932 of 5,447 tracked paths
- **bug**: The domain-first restructure renamed directories case-only on a case-insensitive filesystem, so git never recorded the renames. The index still holds the old casing while disk holds the new, and `git status` reports clean because macOS cannot tell the two apart. The divergence is concentrated in: Assets/Game/Scenes/world (583 paths, disk = World), Assets/Game/Scripts/agents (192, disk = Agents), Assets/Game/Art/Models/{Environment/nature, Vehicles/rover} (95), Assets/Game/Prefabs/agents/{creatures,weapons} (20), Assets/Game/Art/Shaders/caves (19), Assets/Game/Prefabs/VisualEffects/{cutsceneExamples,lines} (16), Assets/Game/Prefabs/Legacy/hostiles (3).
- **failure**: Clone the repo onto a case-sensitive filesystem — Linux CI, a Docker build agent, a Linux/WSL teammate, or an APFS volume created case-sensitive. Git checks out exactly what the index says, producing BOTH Assets/Game/Prefabs/Agents/Vehicles/Aircraft/ (correct casing, from the paths that were properly moved) AND Assets/Game/Prefabs/agents/creatures/ (old casing) as two separate sibling directories, and likewise Scripts/Agents alongside Scripts/agents and Scenes/World alongside Scenes/world. The project hierarchy that dev sees is not the one the author sees. Concretely for this domain: OstrichLocomotionTests.cs:16 loads "Assets/Game/Prefabs/agents/creatures/Ostrich.prefab" — that path resolves on the Linux clone and returns null on the author's Mac, and HumanoidLocomotionTests, HumanoidArmSwingTests, HorseLocomotionTests and HorseRigWiringTests are in the same position. `git status --short` right now proves the index view: it lists `M Assets/Game/Prefabs/agents/creatures/Ostrich.prefab` and `M Assets/Game/Scripts/agents/Modules/Riding/MountedRiderPose.cs`, lowercase, for files that are on disk under Agents/.
- **fix**: Normalize the index to disk before anything else in the cleanup, because every other path fix depends on which casing is authoritative. Per divergent directory: `git mv --force Assets/Game/Scripts/agents Assets/Game/Scripts/Agents-tmp && git mv --force Assets/Game/Scripts/Agents-tmp Assets/Game/Scripts/Agents` (the two-step dance is required on a case-insensitive filesystem), or set core.ignorecase=false and re-add. Commit as one dedicated rename-only commit and rebase open branches onto it. Afterwards the five test files and two builders above can be corrected to a single unambiguous casing.
- **verified**: Compared `git ls-tree -r --name-only HEAD` against disk using a case-EXACT resolver (per-segment os.listdir membership rather than os.path.exists): 932 of 5,447 tracked paths have no exact-case counterpart. Grouped the misses by their first four path segments to get the per-directory counts above, and cross-checked individual cases with os.listdir — e.g. Assets/Game/Prefabs/Agents/Creatures/ on disk contains Ostrich.prefab, HorseRobot.prefab, HumanoidRobot.prefab, CrabWalker{4,6,8}.prefab, MountableAnt.prefab, while `git ls-tree` lists those same seven files under Assets/Game/Prefabs/agents/creatures/. Confirmed the filesystem is case-insensitive by writing CaseTest.txt and reading it back as casetest.txt.

## Assets/Game/Editor/Vehicles/DuneFoilBuilder.cs:1130 (inside EnsureWindPrefab, :1126-1137) against WindPrefabPath at :40
- **bug**: A half-applied fix. WindPrefabPath was correctly updated to "Assets/Game/Prefabs/Environment/Sky/Wind.prefab" (line 40), but EnsureWindPrefab still calls EnsureFolder("Assets/Game/Prefabs/environment") with the old lowercase name — a folder that AssetDatabase does not believe exists, since AssetDatabase.IsValidFolder is case-sensitive.
- **failure**: Delete or never create Assets/Game/Prefabs/Environment/Sky/Wind.prefab, then run Tools ▸ Vehicles ▸ Build Dune Foil Prefab. Line 1128's guard passes (no wind prefab), line 1130 calls EnsureFolder, EnsureFolder line 1175 asks IsValidFolder("Assets/Game/Prefabs/environment") which is false because the imported folder is `Environment`, so line 1179 calls AssetDatabase.CreateFolder("Assets/Game/Prefabs", "environment"). Unity resolves the collision by creating a second folder — typically "environment 1" — leaving a stray empty folder and its .meta in the project. The prefab itself still lands correctly via the Directory.CreateDirectory on line 1134, so nothing errors and nothing warns; you are just left with junk folders accumulating one per run.
- **fix**: Change line 1130 to EnsureFolder(System.IO.Path.GetDirectoryName(WindPrefabPath).Replace('\\','/')) — derives the folder from the one authoritative constant so the two can never drift again. Better still, delete line 1130 entirely: line 1134's Directory.CreateDirectory already creates the tree, making EnsureFolder redundant here. 1 line changed or removed.
- **verified**: Read DuneFoilBuilder.cs:1126-1137 (EnsureWindPrefab) and :1173-1180 (EnsureFolder). Case-exact path walk flagged "Assets/Game/Prefabs/environment" as non-existent while `find Assets/Game/Prefabs -type d` lists Assets/Game/Prefabs/Environment and Assets/Game/Prefabs/Environment/Sky. AssetDatabase path APIs are case-sensitive, which is why the same file's own line 40 was corrected to `Environment`.

## Assets/Game/Editor/Agents/BehaviourModuleEditor.cs:1 and :81-82
- **bug**: Mismatched preprocessor/brace nesting. `#if UNITY_EDITOR` sits on line 1 before the using directives; its `#endif` sits on line 81, INSIDE the namespace block, one line before the namespace's closing brace on line 82. So the conditional region covers the usings, the namespace opener and the whole class, but not the namespace's closing brace.
- **failure**: Compile with UNITY_EDITOR undefined and the file reduces to a single orphan `}` — CS1022, 'Type or namespace definition, or end-of-file expected'. Today this never fires, because the file lives in Assets/Game/Editor/ where UNITY_EDITOR is always defined, which is precisely why the mismatch has gone unnoticed. It becomes a real compile break the moment the file is moved out of an Editor/ folder — for instance during the Editor/Tests consolidation move proposed above — and the resulting error points at a stray brace rather than at the guard that caused it.
- **fix**: Delete line 1 and line 81. The guard is dead weight in an Editor-only assembly and its only current effect is to make the file unsafe to relocate. -2 lines. Apply the same deletion to EntityProfileEditors.cs, which carries the same redundant (though correctly nested) guard.
- **verified**: Read the whole 83-line file. Line 1 is `#if UNITY_EDITOR`; line 6 opens `namespace SpaceGame.EditorTools {`; line 80 closes the class; line 81 is `    #endif`; line 82 is `}` closing the namespace. Confirmed there is no asmdef under Assets/Game/Editor (`ls Assets/Game/Editor/*.asmdef` → no matches), so the file compiles into Assembly-CSharp-Editor where UNITY_EDITOR is always set.

## Assets/Game/Editor/AssetPipeline/IconGenerator.cs:131 (set) and :271-278 (OnDisable)
- **bug**: Closing the Icon Generator window leaves the user's scene camera rendering into a destroyed RenderTexture. DrawPreview assigns renderCamera.targetTexture = previewRT on every OnGUI pass (line 131). OnDisable destroys previewRT (line 277) but never clears targetTexture, so the camera is left holding a dangling reference. Only GenerateIcon resets targetTexture to null (line 245) — the reset lives on the button path, not the teardown path.
- **failure**: Open Tools ▸ Icon Generator. Assign the scene's Main Camera to Render Camera (the ObjectField at line 37 passes allowSceneObjects: true, so a scene camera is the expected input). Preview a prefab, then close the window WITHOUT clicking Generate Icon. OnDisable destroys previewRT while Main Camera.targetTexture still points at it, so Main Camera now renders into a dead target and the Game view goes black. Nothing is logged. Worse, the same code path has already overwritten that camera's clearFlags to SolidColor, backgroundColor to transparent black, allowHDR to false and renderPostProcessing to false (SetupPreviewCamera, lines 138-149), and moved it — ApplyCameraTransform sets position to root + Vector3.back and rotation to Quaternion.identity (lines 111-113) — none of which is restored either. The scene camera is silently reconfigured and displaced, and the scene is left dirty.
- **fix**: In OnDisable, before destroying previewRT: `if (renderCamera != null) renderCamera.targetTexture = null;`. To fix the wider destruction, capture the camera's clearFlags, backgroundColor, allowHDR, renderPostProcessing, position and rotation when it is first assigned and restore them in OnDisable — or stop touching the scene camera at all and render the preview through a HideFlags.HideAndDontSave throwaway camera. Minimum viable fix is 2 lines; the full restore is ~15.
- **verified**: Read IconGenerator.cs in full. Traced the assignment at line 131 (inside DrawPreview, called unconditionally from OnGUI line 65) and confirmed the only reset is at line 245 inside GenerateIcon, which runs solely from the button at line 71. OnDisable (lines 271-278) destroys previewInstance and previewRT with no targetTexture clear. SetupPreviewCamera and ApplyCameraTransform mutate the passed-in Camera directly with no saved original state anywhere in the file.

## ProjectSettings/GraphicsSettings.asset (m_AlwaysIncludedShaders) — affects Assets/Game/Scripts/Presentation/UI/Map/MapHologramTerrain.cs:321,350,735 and UI/HelmetHUD/HelmetMarkerFactory.cs:105,125 and UI/HelmetHUD/HelmetDangerVignette.cs:88
- **bug**: All six shaders that live code looks up at runtime with Shader.Find are unreferenced by any material, scene or prefab, and are absent from Always Included Shaders — so Unity strips them from player builds and every Shader.Find returns null. Unity only includes a shader in a build if it is used by a material reachable from a built scene, sits under Resources/, or is listed in m_AlwaysIncludedShaders; none of these six satisfies any of those.
- **failure**: Make a player build (any platform) and open the map. MapHologramTerrain.Start -> BuildRoot -> Shader.Find("Hologram/Terrain") returns null -> Debug.LogError("[MapHologramTerrain] Shader 'Hologram/Terrain' not found.") at line 328 and the terrain hologram renders nothing at all; the same happens at :359 for "Hologram/Beam" and :738 for "Hologram/Solid", so the beam and every 3D marker also fail. On the helmet HUD, HelmetMarkerFactory:105/125 and HelmetDangerVignette:88 do fall back to Shader.Find("UI/Default"), so those degrade to flat unlit UI instead of disappearing — losing the additive hologram look. None of this reproduces in the editor, which loads all assets regardless of build references, which is why it has gone unnoticed.
- **fix**: Add the six shaders to m_AlwaysIncludedShaders in ProjectSettings/GraphicsSettings.asset — zero code lines. Their GUIDs: HologramTerrain.shader e0aa91809ee1f4373b6b98250a791dcd, HologramBeam.shader c24f52d2a4c0b4ba39f664f66d6d21ed, HologramSolid.shader 32da6b7a327ad4c2a9cccc7ddb15c9d3, HelmetHUDHolographicUI.shader 192c60b18bae440b48a1990e59f14214, HelmetHUDHologramSolid.shader 757b0a2c5fc604518a91c1c84f2036e0, HelmetHUDDangerVignette.shader f9f7cd701419e456cbcb7fa909169fab. (Do NOT add MapHolographic.shader — it is on the removal list.) The alternative, authoring six Material assets and serialising them onto the components, is more lines and more wiring for the same effect.
- **verified**: For each of the six GUIDs, `grep -rl <guid> Assets/ ProjectSettings/` returns ONLY that shader's own .meta file — no material, scene, prefab or settings file. `grep -A25 m_AlwaysIncludedShaders ProjectSettings/GraphicsSettings.asset` lists exactly 7 entries, all with guid 0000000000000000f000000000000000 (built-in Unity shaders). Confirmed each declared shader name matches the Shader.Find string by grepping `Shader "` in each .shader: "Hologram/Terrain", "Hologram/Beam", "Hologram/Solid", "UI/HelmetHUDHolographic", "UI/HelmetHUDHologramSolid", "UI/HelmetHUDDangerVignette".

## Assets/Game/Resources/MapMeshes/ (content gap) — surfaces at Assets/Game/Scripts/Presentation/UI/Map/MapHologramTerrain.cs:786-793
- **bug**: Only 36 of the 48 baked chunk meshes the live world config requires exist, so 12 chunks of the map hologram are permanently blank and log a warning on every startup. Baked meshes cover x=2..7, y=0..5; WorldStreamingConfig.asset defines 48 chunks at x=0..7, y=0..5. The entire x=0 and x=1 columns are missing.
- **failure**: Enter play in persistentScene. MapHologramTerrain.prefab has revealAllChunks: 1 (line 93 of the prefab), so SubscribeToMapService takes the `if (revealAllChunks || svc == null)` branch at line 766 and calls OnChunkRevealed for every one of the config's 48 chunks. For the 12 coords MapMesh_0_0..MapMesh_1_5, Resources.Load<Mesh> at line 789 returns null, line 792 logs '[MapHologramTerrain] Missing baked mesh: Resources/MapMeshes/MapMesh_0_0.asset ...' and the method returns without spawning geometry. Result: 12 warnings every startup and the western quarter of the map hologram is a hole. Deterministic, no player action needed.
- **fix**: Re-run Tools -> World Streaming -> Bake Map Meshes (Assets/Game/Editor/Map/MapMeshBaker.cs, output Assets/Game/Resources/MapMeshes) across the full 8x6 grid to produce the missing 12 assets. This is a content bake, not a code change. IMPORTANT: this also means Resources/MapMeshes must NOT be deleted despite all 36 assets showing refcount 0 in refs.tsv and appearing in orphans.tsv — they are loaded by string path at MapHologramTerrain.cs:789, and the scanner cannot see Resources.Load. That folder is 23 MB and is the single largest live-but-zero-ref asset group in my scope.
- **verified**: Listed Assets/Game/Resources/MapMeshes/*.asset and extracted coords: 36 files, x in {2,3,4,5,6,7} x y in {0..5}. Extracted chunk names from Assets/Game/Settings/WorldStreamingConfig.asset: 48 unique Chunk_x_y with x 0-7, y 0-5. Read MapHologramTerrain.cs:753-800 to confirm the all-chunks branch enumerates config.chunks, and grepped `revealAllChunks` in MapHologramTerrain.prefab to confirm it is serialised to 1.

## Assets/Game/Scripts/Presentation/UI/Buttons/UIButton.cs:45 and :61
- **bug**: AudioManager.Instance is dereferenced without a null guard, so hovering or clicking a menu button throws a NullReferenceException in any session that did not boot through Bootstrap.unity. AudioManager exists only on Assets/Game/Prefabs/Systems/AudioManager.prefab, which refs.tsv places in exactly two scenes — Bootstrap.unity and 'Marius test scene.unity'. MainMenu.unity does not contain it and depends entirely on AudioManager's DontDestroyOnLoad surviving from Bootstrap.
- **failure**: Open Assets/Game/Scenes/Core/MainMenu.unity in the editor and press Play (the normal way to iterate on the menu, skipping Bootstrap). Hover any 'Menu Button' prefab instance -> OnPointerEnter fires -> AudioManager.Instance is null -> NullReferenceException at UIButton.cs:45. Because the exception aborts the handler before line 47, SetState(ButtonState.Highlighted) never runs, so the button also silently loses its hover animation — the visible symptom is 'menu buttons don't highlight', which does not point at audio. Same at :61 for OnPointerDown.
- **fix**: Change both call sites to `AudioManager.Instance?.PlayEvent(hoverSound);` and `AudioManager.Instance?.PlayEvent(pressSound);` — zero net lines. Scoped deliberately: I verified Bootstrap.unity is the FIRST entry in ProjectSettings/EditorBuildSettings.asset m_Scenes, so shipped builds always create AudioManager and the shipped path is safe. This is an editor-workflow defect only, which is why I rate it low severity. Note the sibling concern is not a bug: UIButton.SetState dereferences `animator` unguarded, but animator is wired in Menu Button.prefab (fileID 470731945719654683), so that path is fine.
- **verified**: Read UIButton.cs in full. Looked up Assets/Game/Prefabs/Systems/AudioManager.prefab in refs.tsv: refcount 2, referencing files Bootstrap.unity and 'Marius test scene.unity' only. Confirmed AudioManager.cs:42 calls DontDestroyOnLoad and :10 exposes a plain public static Instance with no auto-creation. Confirmed Bootstrap.unity is scene index 0 via `grep -A3 m_Scenes: ProjectSettings/EditorBuildSettings.asset`. Confirmed the animator field is non-null in the prefab by reading the UIButton serialised block at Menu Button.prefab:293-315.

## ProjectSettings/GraphicsSettings.asset m_AlwaysIncludedShaders — affects Assets/Game/Scripts/Presentation/UI/Map/MapHologramTerrain.cs:321,350,735 and Assets/Game/Scripts/Presentation/UI/HelmetHUD/{HelmetMarkerFactory.cs:105,125, HelmetDangerVignette.cs:88}
- **bug**: Seven project shaders are obtained exclusively through Shader.Find() at runtime, are referenced by no material/prefab/scene, and are absent from Always Included Shaders — so Unity's shader stripper excludes them from player builds and every Shader.Find returns null in the shipped game.
- **failure**: Build a player. persistentScene (in EditorBuildSettings) instantiates Prefabs/VisualEffects/Hologram/MapHologramTerrain.prefab. Press the map toggle -> MapHologramTerrain.cs:321 Shader.Find("Hologram/Terrain") returns null -> line 328 logs "[MapHologramTerrain] Shader 'Hologram/Terrain' not found." and the 36 baked chunk meshes render with a null material (magenta/invisible). Same for Hologram/Beam (line 350) and Hologram/Solid (line 735). In the helmet HUD, UI/HelmetHUDHolographic and UI/HelmetHUDHologramSolid fall back to UI/Default so nav markers lose the hologram look, and UI/HelmetHUDDangerVignette falls back so the damage vignette does not render. All of it works perfectly in the Editor, which is why it has not been noticed.
- **fix**: Add the 7 shaders to Project Settings > Graphics > Always Included Shaders (m_AlwaysIncludedShaders in ProjectSettings/GraphicsSettings.asset): Art/Shaders/UI/Map/{HologramTerrain,HologramBeam,HologramSolid,MapHolographic}.shader and Art/Shaders/UI/HelmetHUD/{HelmetHUDHolographicUI,HelmetHUDHologramSolid,HelmetHUDDangerVignette}.shader.
- **verified**: Parsed m_AlwaysIncludedShaders out of ProjectSettings/GraphicsSettings.asset: all 7 existing entries use guid 0000000000000000f000000000000000 (Unity built-in resources), none is a project shader. Cross-checked each of the 7 .shader files' declared Shader "..." name against every Shader.Find call site in Assets/**/*.cs — exact matches on all 7. Confirmed all 7 are in orphans.tsv (GUID appears in no other file), so no material or scene keeps them alive either. The scripts themselves document the hazard: HelmetMarkerFactory.cs:110 says "Add the shader to Project Settings ...".

## Assets/Game/Scripts/Core/Persistence/Runtime/SaveablePrefabRegistry.cs:25 and :66
- **bug**: The saveable-prefab registry loads from Resources.LoadAll<GameObject>("Saveable") but no Resources/Saveable folder exists anywhere in the project, so the registry is permanently empty and no dynamically-spawned saved object can ever be reconstructed on load.
- **failure**: ResourcesFolder is the constant "Saveable"; line 66 iterates Resources.LoadAll<GameObject>("Saveable"). ls of Assets/Game/Resources shows exactly five subfolders — Cameras, Interiors, Items, MapMeshes, MapTiles — and a repo-wide `find Assets -type d -name Saveable` returns nothing (the only other Resources folders are Assets/Plugins/FMOD/Resources and Assets/ThirdParty/TextMesh Pro/Resources). LoadAll on a non-existent folder returns an empty array, so the registry has zero entries. Consequence: save the world, restart, load -> WorldSaveStore cannot resolve any saved prefab id and hits its own diagnostic at WorldSaveStore.cs:269 ("...Resources/{SaveablePrefabRegistry.ResourcesFolder}?"). Every dropped item / spawned vehicle recorded in the save is silently lost.
- **fix**: Create Assets/Game/Resources/Saveable/ and place (or symlink via a prefab variant) every prefab that carries a Saveable component in it — the Editor validator Scripts/Core/Persistence/Editor/SaveWiringValidator.cs:60 already checks this folder and would report the correct list. This is the one genuinely-needed NEW folder in the hierarchy proposal.
- **verified**: Read the constant and the LoadAll call, then enumerated Assets/Game/Resources on disk and ran a repo-wide directory search for 'Saveable' — no such folder. Flagging to the parent because it spans my scope (missing asset folder) and the persistence auditor's (which prefabs belong in it).

## ProjectSettings/EditorBuildSettings.asset — entries for Assets/Game/Scenes/Interiors/TestInterior.unity and Assets/Game/Scenes/Interiors/InsideRuin.unity
- **bug**: Two scene entries are marked enabled: 1 but the scene files do not exist, so the build contains two null SceneAsset slots and every build index after them is offset from what a reader of the list would expect.
- **failure**: Parsed all 263 entries; 2 have enabled: 1 and a path that fails os.path.exists — Assets/Game/Scenes/Interiors/TestInterior.unity (position 2) and Assets/Game/Scenes/Interiors/InsideRuin.unity (position 12). Neither appears in `git ls-files Assets/Game/Scenes` either, so they were deleted without cleaning the build list. Any SceneManager.LoadScene(int) or GetSceneByBuildIndex arithmetic against the visible list is wrong by two, and Unity emits a build warning per missing scene. Related dead reference: Scripts/Core/SceneManagement/Interiors/InteriorTestBootstrap.cs:32 does Resources.Load<InteriorScene>("Interiors/Interior_Test") and no Interior_Test asset exists (repo-wide `find Assets -name 'Interior_Test*'` is empty), so that bootstrap always no-ops — the same deleted-test-interior feature, half-removed in two places.
- **fix**: Delete the two entries from ProjectSettings/EditorBuildSettings.asset. Bootstrapper.cs:21's SceneManager.LoadScene(0) is unaffected because index 0 (Bootstrap.unity) precedes both. Separately delete InteriorTestBootstrap.cs or restore an Interior_Test asset (scripts auditor's call).
- **verified**: Regex-parsed every (enabled, path) pair from EditorBuildSettings.asset and tested each path with os.path.exists plus `git ls-files`. Confirmed 248 chunk entries + 15 non-chunk, of which exactly these 2 are missing from both disk and git.

## Assets/Game/Scenes/Minigames/MinigameArena.unity (missing MatchManager) vs Assets/Game/Scripts/Gameplay/Minigame/Runtime/MatchManager.cs:15
- **bug**: MatchManager — the component that owns the entire minigame match (teams, bot spawning, win condition) — is not present in any scene or prefab in the project, so selecting the minigame from the main menu loads an arena with no match logic and no bots. This is also why Prefabs/Agents/Robots/DeathmatchBot.prefab shows 0 references.
- **failure**: MatchManager.cs:15 documents "in MinigameArena.unity" and Scripts/Gameplay/Game/Spawning/SpawnManager.cs:203 does FindFirstObjectByType<MatchManager>(). Walked every file under Assets/ and ProjectSettings/ for MatchManager's script GUID 4e26c1883c39a42b09ab1d3accec3e16: the only hit is its own .cs.meta. refs.tsv agrees (refcount 0), as it does for the whole Gameplay/Minigame subtree (MatchEnums, MatchRules, MatchWinEvaluator, SpawnReachability, TeamAssignment, MatchScoreEntry, MatchSettings, MinigameConfigUI — all 0). Repro: MainMenu -> minigame button -> MainMenuUI.cs:96 NetworkManager.Singleton.SceneManager.LoadScene(minigameScene.SceneName, Additive) loads the 14 MB MinigameArena; SpawnManager's FindFirstObjectByType<MatchManager>() returns null, no teams are assigned, no bots spawn (MatchManager.cs:226's "No DeathmatchBot prefab assigned" error never even prints because the component does not exist), and no win condition can ever fire.
- **fix**: Add a MatchManager to MinigameArena.unity and assign deathmatchBotPrefab = Prefabs/Agents/Robots/DeathmatchBot.prefab, teamFactions = ScriptableObjects/Factions/Teams/*, soloFactions = Factions/Solo/*, relationshipTable = Factions/Core/GlobalRelationships.asset, arenaTargetingProfile. Do NOT delete DeathmatchBot.prefab — its orphan status is the symptom, not the cause.
- **verified**: Full-repo GUID scan for the MatchManager script GUID (one hit: its own .meta), cross-checked against refs.tsv refcount 0 for MatchManager.cs and every other file in Scripts/Gameplay/Minigame/. Read MatchManager.cs's [SerializeField] list to confirm deathmatchBotPrefab is a serialized field that only scene/prefab data can populate.

## Assets/Game/Resources/Items/Artifacts/Leash.asset and Assets/Game/Resources/Items/Artifacts/RocketTurret.asset (itemName field)
- **bug**: Two registry-visible InventoryItem assets still carry InventoryItem.cs's default itemName = "NewItem", so both items display as "NewItem" in the inventory UI.
- **failure**: InventoryItem.cs:24 declares `public string itemName = "NewItem";`. Resources/Items/Artifacts/Leash.asset has itemName: NewItem and RocketTurret.asset has itemName: NewItem. Both are loaded into the registry by RegistryLoader.cs:19 Resources.LoadAll<InventoryItem>("Items"). Scripts/Presentation/UI/Pages/DevInventoryUI.cs:644 returns `string.IsNullOrWhiteSpace(item.itemName) ? item.name : item.itemName` — "NewItem" is not whitespace, so the fallback to the asset name never triggers. Pick up the Leash or the rocket turret in game and the inventory slot reads "NewItem"; InventorySlot.cs:20's ToString does the same. The duplicate outside Resources (ScriptableObjects/Items/Leash.asset) has the correct itemName 'Leash', which is how the mistake stayed hidden.
- **fix**: Set itemName to 'Leash' and 'Rocket Turret' in the two Resources assets. Also note Leash.asset's icon guid (e4428f69…) is the Lasso icon, not Sprites/Items/Leash.png (e8e082f4…, used only by the ScriptableObjects duplicate) — so the leash currently shows the lasso's picture too.
- **verified**: Dumped itemName/itemPrefab/icon from all 10 Resources/Items/Artifacts assets and all 6 ScriptableObjects/Items assets, matched them by itemPrefab fileID+guid, and traced itemName's consumers with a repo-wide grep of .cs (13 hits; the two display sites are DevInventoryUI.cs:644 and InventorySlot.cs:20).

## git tree vs working tree — 1060 paths under Assets/Game, notably Assets/Game/Scenes/world/ (git) vs Assets/Game/Scenes/World/ (disk, and what EditorBuildSettings says)
- **bug**: The repository records directory names in a different case than the files on disk and than the paths in ProjectSettings/EditorBuildSettings.asset. On macOS's case-insensitive filesystem this is invisible; on a case-sensitive filesystem (Linux CI, a Linux/WSL teammate) the clone produces the lowercase directories and every capitalised path fails to resolve.
- **failure**: `git ls-files Assets/Game/Scenes` reports 565 files under 'Assets/Game/Scenes/world/Chunks', plus 'Assets/Game/Scenes/world/persistentScene.unity' and 'world/MasterScene.unity' — lowercase w. `find` on disk reports 'Assets/Game/Scenes/World/...'. EditorBuildSettings.asset lists 'Assets/Game/Scenes/World/persistentScene.unity' and 248 'Assets/Game/Scenes/World/Chunks/Chunk_x_y.unity'. Clone on Linux -> only Scenes/world/ exists -> all 249 World-cased build entries point at nothing -> the game has no persistent scene and no streaming chunks. Same class of defect elsewhere: git has Assets/Game/Prefabs/Legacy/hostiles/ (disk: Hostiles/), Assets/Game/Prefabs/agents/creatures/ (disk: Agents/Creatures/) which is the path 5 EditMode tests hardcode (e.g. Tests/EditMode/HumanoidLocomotionTests.cs:30 "Assets/Game/Prefabs/agents/creatures/HumanoidRobot.prefab"), Assets/Game/Prefabs/VisualEffects/cutsceneExamples/, Assets/Game/Prefabs/VisualEffects/lines/, Assets/Game/Art/Shaders/caves/, and ~200 paths under Assets/Game/Scripts/agents/.
- **fix**: Normalise with case-only renames that git records: `git mv Assets/Game/Scenes/world Assets/Game/Scenes/tmpworld && git mv Assets/Game/Scenes/tmpworld Assets/Game/Scenes/World`, repeated for Prefabs/Legacy/hostiles, Prefabs/agents, Prefabs/VisualEffects/{cutsceneExamples,lines}, Art/Shaders/caves and Scripts/agents. Also set core.ignorecase=false locally so this stops recurring. Then align the 5 hardcoded test paths to the canonical case.
- **verified**: Walked every `git ls-files Assets/Game` path component against os.listdir of the real directory and counted mismatches: 1060 paths, grouped by prefix (565 Scenes/world/Chunks, 84+43+... Scripts/agents/*, 28 Prefabs/agents/creatures, 10 VisualEffects/cutsceneExamples, 4 VisualEffects/lines, 18 Art/Shaders/caves, 2 Prefabs/Legacy/hostiles). Cross-checked the EditorBuildSettings paths use the capitalised form. Reporting because the asset tree is where the damage lands, though the Scripts/agents half belongs to the scripts auditor.

## Assets/Game/Scripts/Core/SceneManagement/Core/SceneReference.cs:2-7 and 32
- **bug**: The `namespace SpaceGame.Core` declaration and its opening brace are INSIDE an `#if UNITY_EDITOR` block whose `#endif` sits on line 7, after the opening brace. The matching closing brace on line 32 is unconditional. So with UNITY_EDITOR undefined the compiler sees a stray `}` at end of file and the class is no longer in a namespace.
- **failure**: Build a player (File > Build, any non-editor target, or a headless/dedicated-server build). UNITY_EDITOR is undefined, so the preprocessor drops lines 3-6 (`using UnityEditor;`, blank, `namespace SpaceGame.Core`, `{`) while keeping the `}` on line 32. Result: CS1022 'Type or namespace definition, or end-of-file expected' at SceneReference.cs(32,1), and the build fails. Even if the brace balanced, `SceneReference` would land in the global namespace and GameManager.cs:21 (`[SerializeField] private SceneReference onWinScene;`, in namespace SpaceGame.Gameplay with `using SpaceGame.Core;`) would fail to resolve it. This file is live: 6 SceneReference assets exist under Assets/Game/Scenes/References/ and GameManager.prefab points at Main Menu.asset.
- **fix**: Close the conditional immediately after the using it guards and open the namespace unconditionally: line 2-4 become `#if UNITY_EDITOR` / `using UnityEditor;` / `#endif`, then a blank line, then `namespace SpaceGame.Core` / `{`, and delete the stray `#endif` on line 7. Net -1 line.
- **verified**: Dumped the file with exact line numbers and repr() to confirm the #endif on line 7 is after the `{` on line 6. Then wrote a preprocessor-aware brace-balance scanner over every non-vendor .cs in Assets/: with UNITY_EDITOR defined the file balances at 0, without it balances at -1. Only 5 files in the repo show the asymmetry; the two under Assets/Game/Editor/ are in an editor-only assembly where UNITY_EDITOR is always defined, so they are harmless.

## Assets/Game/Scripts/Core/SceneManagement/Interiors/InteriorScene.cs:2-8 and 63
- **bug**: Identical defect to SceneReference.cs: `namespace SpaceGame.Core {` on lines 6-7 is inside `#if UNITY_EDITOR`, whose `#endif` is on line 8; the closing `}` on line 63 is unconditional.
- **failure**: Same build: UNITY_EDITOR undefined drops lines 3-7, leaving the `}` on line 63 unmatched -> CS1022 at InteriorScene.cs(63,1) and the build fails. InteriorScene is load-bearing at runtime: Interior_AlgeaCave.asset and Interior_SandstoneCave.asset are instances of it, InteriorManager.EnterInterior reads def.SceneName/def.SpawnAnchorId, InteriorEntrance serializes one, and InteriorSceneDestination serializes one. So the interior system cannot exist in a build at all.
- **fix**: Same shape: `#if UNITY_EDITOR` / `using UnityEditor;` / `#endif` on lines 2-4, drop the now-unused `using SpaceGame.World;` on line 4 (nothing in the file references any SpaceGame.World type — verified), then open the namespace unconditionally and delete the `#endif` on line 8. Net -2 lines.
- **verified**: Same exact-line dump plus the brace-balance scanner: balances at 0 with UNITY_EDITOR, -1 without. Also grepped the body for WorldStreamer/SceneTracked/WorldChunk to confirm the SpaceGame.World import is unused (0 hits).

## ProjectSettings/EditorBuildSettings.asset:11-13 (index 1) and index 202
- **bug**: Two `enabled: 1` scene entries point at scene files that do not exist on disk: index 1 = Assets/Game/Scenes/Interiors/TestInterior.unity and index 202 = Assets/Game/Scenes/Interiors/InsideRuin.unity.
- **failure**: Any player build enumerates EditorBuildSettings and cannot find the asset behind those entries. In the editor, `SceneManager.LoadSceneAsync(1)` (which Bootstrapper.cs does on every launch — see the next bug) resolves to an entry with no file and fails; at build time Unity either errors on the missing scene or strips it, and stripping SHIFTS every later build index, so any code addressing a scene by index silently targets the wrong scene. `ls Assets/Game/Scenes/Interiors/` contains only AlgeaCave.unity and SandstoneCaveInterior.unity (plus the AlgeaCave/ and CaveBakes/ lighting folders).
- **fix**: Remove both entries from the m_Scenes list. Separately worth a decision: indices 4-8 are five personal test scenes (Blocking test, Aleksander/Tommy/Emil/Marius test scene) enabled AHEAD of persistentScene at index 9, so they ship in every player build. Disabling them (enabled: 0) keeps them openable in the editor while dropping them from builds.
- **verified**: Parsed all 263 `- enabled/path/guid` triples out of EditorBuildSettings.asset with a script and os.path.exists()'d each path against the repo. Exactly two paths are missing, both enabled, at indices 1 and 202.

## Assets/Game/Scripts/Core/SceneManagement/Core/Bootstrapper.cs:28-31
- **bug**: `if (targetScene <= 0) targetScene = 1; // Default to main menu if no target specified` — build index 1 is not the main menu. Index 1 is Assets/Game/Scenes/Interiors/TestInterior.unity (which is also a deleted file); MainMenu.unity is index 2. This runs from a live `[RuntimeInitializeOnLoadMethod(AfterSceneLoad)]`, unconditionally, in every play session and every build.
- **failure**: Launch a build, or press Play in the Bootstrap scene (build index 0). BeforeSceneLoad returns early or records targetScene as 0/-1; AfterSceneLoad then takes the `targetScene <= 0` branch, sets targetScene = 1, and calls `SceneManager.LoadSceneAsync(1, LoadSceneMode.Single)`. The intended MainMenu (index 2) is never loaded, and index 1's scene file does not exist, so the load fails or lands somewhere unintended instead of the main menu.
- **fix**: Preferred: delete Bootstrapper.cs (see the removal entry — it has 0 references and duplicates NetworkBootstrap's job). If it is kept, stop hardcoding indices — resolve the menu scene through the existing SceneReference asset (Assets/Game/Scenes/References/Main Menu.asset) or at minimum change the literal to 2 and fix the two dead EditorBuildSettings entries first, since any index literal is invalidated by them.
- **verified**: Read Bootstrapper.cs in full and traced both RuntimeInitializeOnLoadMethod entry points. Cross-checked the index-to-path mapping against ProjectSettings/EditorBuildSettings.asset (index 0 Bootstrap, 1 TestInterior [missing], 2 MainMenu, 3 LobbyMenu) with the same parser used for the previous bug.

## Assets/Game/Scripts/Core/GameServices/Implementations/PlayerDropService.cs:8,18-23
- **bug**: `[Rpc(SendTo.Server)] private void DropServerRpc(...)` is declared on `public class PlayerDropService : IItemDropService` — a plain C# class, not a NetworkBehaviour. Netcode's RPC code generation only rewrites methods on NetworkBehaviour subclasses, so the attribute is inert and DropServerRpc is compiled and invoked as an ordinary local method call.
- **failure**: A non-host client drops an item from its inventory (EquipmentController.cs:169 or EntityLootTable.cs:64/78 -> GameServices.ItemDropService.DropItem). Network.Execute takes the `client` branch because Network.Server is false, and calls DropServerRpc — which executes locally on that client instead of on the server. It then runs Drop(), which does `Object.Instantiate(item.itemPrefab, ...)` and, because Network.IsNetworked is true, `obj.GetComponent<NetworkObject>().Spawn()`. Spawn() from a client throws (only the server may spawn), so the item leaves the client's inventory, a local-only ghost prefab is left in the scene, and no other peer ever sees the drop. The host path is unaffected, which is why singleplayer-as-host never surfaces it — matching the known 'unregistered network prefabs fail ONLY on clients' pattern in this project.
- **fix**: Move the drop authority onto a NetworkBehaviour. Cheapest option that adds no new file: give PlayerDropService a reference to an existing scene NetworkBehaviour that already owns spawn authority (SpawnManager or NetworkGameManager, both singletons in persistentScene) and route the client branch through an [Rpc(SendTo.Server)] method there. Do NOT just delete the attribute — that leaves the client silently spawning locally.
- **verified**: Read PlayerDropService.cs and confirmed the class derives only from IItemDropService. Confirmed Netcode's generator is scoped to NetworkBehaviour types by inspecting Library/PackageCache/com.unity.netcode.gameobjects@e40dcfef2dee/Editor/CodeGen/NetworkBehaviourILPP.cs (the only ILPP that reads RpcAttribute; it walks NetworkBehaviour type definitions). Traced Network.Execute in Core/Multiplayer/Networking.cs:23-35 to confirm which branch a client takes.

## Assets/Game/Scripts/Gameplay/Health/NetworkedHealthComponent.cs:33-49
- **bug**: `public override void OnDestroy()` never calls `base.OnDestroy()` — on either path. It also returns early on line 35 when `health == null`, i.e. whenever the object is destroyed before OnNetworkSpawn ran.
- **failure**: Any PlayerCharacterNetworked instance is destroyed — a client disconnecting, a Single-mode scene load, or a quickload (SaveHotkeys.QuickLoad reloads the world scene). NetworkBehaviour.OnDestroy (Library/PackageCache/com.unity.netcode.gameobjects@e40dcfef2dee/Runtime/Core/NetworkBehaviour.cs:1679-1713) is what calls InternalOnDestroy(), NetworkObject.OnNetworkBehaviourDestroyed(this), and `foreach (var networkVar in NetworkVariableFields) networkVar.Dispose();`. Because it is never invoked, this component's `NetworkVariable<int> networkHealth` is never disposed and the destroyed behaviour is never removed from its NetworkObject's ChildNetworkBehaviours list, so NGO keeps a reference to a dead component. Repeated join/leave or quickload cycles accumulate both leaks.
- **fix**: Restructure so the base call always runs: guard only the unsubscribes, then call base.OnDestroy() unconditionally at the end — i.e. change the line 35 early return to `if (health != null) { ...unsubscribes... }` followed by `base.OnDestroy();`. SingleplayerInitializer.cs:35-43 already does exactly this and is the in-repo pattern to copy.
- **verified**: Read the NGO 2.x NetworkBehaviour.OnDestroy source in this project's own PackageCache to confirm what the base call does (lines 1679-1713, ending in the NetworkVariable Dispose loop). Then grepped every `override void OnDestroy` in Assets/Game/Scripts for a following base.OnDestroy(): SingleplayerInitializer.cs:35 has it; NetworkedHealthComponent.cs:33 does not. One more offender outside my scope, Assets/Game/Scripts/Items/Inventory/Components/PlayerInventoryNetwork.cs:56, has the same problem — flagging it for whoever owns Items/.


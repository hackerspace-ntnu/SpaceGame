namespace SpaceGame.Presentation.Lobbies
{
    /// <summary>Which wait the join page is currently showing, if any.</summary>
    public enum LobbyBusyScope { None, SigningIn, Querying, JoiningByCode, JoiningRow }
}
